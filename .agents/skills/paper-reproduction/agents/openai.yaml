interface:
  display_name: "Paper Reproduction"
  short_description: "Reproduce, run, tune, and compare saved paper projects"
  default_prompt: "Use $paper-reproduction to continue the saved paper reproduction Project from current artifacts and status."

policy:
  allow_implicit_invocation: true
