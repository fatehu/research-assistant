# 全面审查_动态测试证据_2026-02-24_00-40

## 一、执行命令与结果
1. 后端检查
- `python backend/checks/check_no_new_broad_excepts.py` -> PASS
- `python backend/checks/check_contract_alignment.py` -> PASS

2. 前端质量
- `npm run lint` -> PASS（warnings=14）
- `npm run build` -> PASS（chunk warning）

3. 容器
- `docker compose up -d --build backend frontend` -> PASS
- `docker compose ps` -> PASS
- `docker compose logs --tail 200 backend/frontend` -> PASS

4. E2E
- `acceptance_tests.ps1` -> PASS
- `role_business_api_smoke_e2e.ps1` -> PASS
- `remaining_modules_api_smoke_e2e.ps1` -> FAIL -> FIX -> PASS
- `student_role_smoke_e2e.ps1` -> PASS
- `role_route_smoke_e2e.ps1 -Role mentor` -> FAIL -> FIX -> PASS
- `role_route_smoke_e2e.ps1 -Role admin` -> PASS
- `mcp_settings_login_e2e.ps1` -> FAIL -> FIX -> PASS
- `knowledge_upload_pipeline_e2e.ps1` -> PASS

## 二、关键失败证据
1. 上传失败
- 现象：`/knowledge/.../documents/upload` 返回 500。
- 日志：`PermissionError: [Errno 13] Permission denied: '/app/uploads/<uid>'`。
- 归类：代码缺陷（容器权限策略不完整）。

2. MCP 保存失败
- 现象：`/api/v1/mcp/config` PUT 无 2xx 响应。
- 日志：`PermissionError: [Errno 13] Permission denied: 'mcp_servers.json'`。
- 归类：代码缺陷（容器权限策略不完整）。

3. 角色路由脚本失败
- 现象：`playwright-cli open http://localhost:3000/login` 间歇失败。
- 报错：`Target page closed / Timeout / ERR_EMPTY_RESPONSE`。
- 归类：脚本缺陷（初始化流程不稳）。

## 三、修复映射
1. `backend/docker-entrypoint.sh`
- 增加 `/app/uploads`、`/app/model_cache` 权限修复。
- 增加 `MCP_CONFIG_PATH` 文件创建和授权。

2. `backend/Dockerfile`
- 引入 `gosu` + entrypoint，保持最终业务进程非 root。

3. `e2e/role_route_smoke_e2e.ps1`
- 新增浏览器 open 重试。
- 改为 `open about:blank` + `run-code` 跳转登录页。

## 四、当前状态
- 全链路脚本已打通，容器健康，上传/配置写入恢复。
- 后端全集单测仍有历史遗留失败（已在阶段测试记录单列）。
