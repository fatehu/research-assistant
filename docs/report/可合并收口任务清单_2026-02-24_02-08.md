# 可合并收口任务清单（Merge Readiness Checklist）

- 时间：2026-02-24 02:08
- 更新：2026-02-24 02:19
- 分支：`feature/full-remediation-2026-02`
- 目标：将当前分支推进到可 PR Merge 的工程质量门槛。

## 验收门槛（必须全部满足）

1. 后端门禁通过：
   - `python backend/checks/check_no_new_broad_excepts.py`
   - `python backend/checks/check_contract_alignment.py`
2. 后端全集测试通过：
   - `docker compose exec backend python -m pytest tests -q`
3. 前端质量通过：
   - `npm run lint`
   - `npm run build`
4. 容器健康：`docker compose ps` 核验核心服务均为 `Up/healthy`。

## 任务清单（完成一项即打勾）

- [x] 01 冻结当前阻塞基线（复现并记录失败证据）
  - 已执行：`docker compose exec backend python -m pytest tests/test_rate_limit_dependency.py -q`
  - 结果：失败（期望第三次 429，实际 200）
  - 已执行：导出兼容探测脚本
  - 结果：`SemanticChunker/AcademicStructureDetector/HierarchicalChunker/_empty_result` 均缺失（False）

- [x] 02 修复 `smart_chunking_service` 兼容导出
  - 完成：恢复 `SemanticChunker`、`AcademicStructureDetector`、`HierarchicalChunker` 导出。
  - 文件：`backend/app/services/smart_chunking/__init__.py`

- [x] 03 恢复 `SmartChunkingService._empty_result` 兼容方法
  - 完成：补齐 `_empty_result()`，返回历史兼容结构。
  - 文件：`backend/app/services/smart_chunking/service.py`

- [x] 04 修复限流依赖稳定性（memory 模式下必须严格限流）
  - 完成：`build_rate_limit_dependency` 改为依赖级 `APIRateLimiter` 实例，避免模块级单例与 monkeypatch 配置冲突。
  - 文件：`backend/app/core/rate_limit.py`

- [x] 05 运行针对性回归并清零本轮失败
  - 命令：
    - `docker compose exec backend python -m pytest tests/test_semantic_boundary.py -q`
    - `docker compose exec backend python -m pytest tests/test_smart_chunking_full.py -q`
    - `docker compose exec backend python -m pytest tests/test_rate_limit_dependency.py -q`
  - 结果：全部通过。

- [x] 06 运行后端全集测试并通过
  - 命令：`docker compose exec backend python -m pytest tests -q`
  - 结果：`276 passed`。

- [x] 07 前端质量复核
  - 命令：`npm run lint`、`npm run build`
  - 结果：通过（保留 14 条既有 warning，无 error）。

- [x] 08 容器健康与关键链路冒烟
  - 命令：`docker compose ps`、`docker compose logs --tail 100 backend`、`docker compose logs --tail 100 frontend`
  - 端到端：
    - `./acceptance_tests.ps1` 通过
    - `./e2e/knowledge_upload_pipeline_e2e.ps1` 通过（upload -> processing -> search）

- [x] 09 文档补齐（阶段双文档）
  - `docs/skills/全面审查整改_阶段15_可合并收口_2026-02-24_02-19.md`
  - `docs/test/全面审查整改_阶段15_可合并收口测试记录_2026-02-24_02-19.md`

- [x] 10 提交与推送（中文 commit）
  - 提交前：`git status` / `git diff` / `git diff --cached`
  - 目标：单 commit 仅包含“本阶段收口”相关文件。

## 当前结论

当前状态：**达到可合并门槛（Go for PR Merge）**。
