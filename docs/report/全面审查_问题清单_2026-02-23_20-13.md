# 全面审查问题清单（2026-02-23 20:13）

## 0. 审查结论概览
- 审查模式：全仓全链路（静态 + 动态）
- 严重级别汇总：
  - `P0`: 0
  - `P1`: 5
  - `P2`: 7
  - `P3`: 1
- 结论：当前系统“核心链路可运行”，但存在契约一致性、可测试性、测试稳定性、并发一致性与安全基线等中高风险问题。

## 1. P0（阻断级）
本轮未发现可稳定复现的 P0（数据损坏/核心链路不可用/直接安全失陷）。

## 2. P1（高优先级）

### P1-01 状态枚举契约未真正统一
- 风险等级：`P1`
- 影响范围：知识库文档状态、文献链接状态、前后端契约联动
- 文件与行号：
  - `backend/app/models/knowledge.py:38`
  - `backend/app/models/knowledge.py:40`
  - `backend/app/models/knowledge.py:41`
  - `backend/app/models/knowledge.py:42`
  - `backend/app/models/literature.py:46`
  - `backend/app/models/literature.py:48`
  - `backend/app/models/literature.py:49`
  - `backend/app/models/literature.py:50`
  - `backend/alembic/versions/017_status_normalization_and_conflict_cleanup.py:51`
  - `backend/alembic/versions/017_status_normalization_and_conflict_cleanup.py:53`
  - `backend/alembic/versions/017_status_normalization_and_conflict_cleanup.py:85`
  - `backend/alembic/versions/017_status_normalization_and_conflict_cleanup.py:87`
  - `frontend/src/services/api.ts:17`
  - `frontend/src/services/api.ts:62`
  - `frontend/src/services/api.ts:63`
- 证据：
  - 前端 `TaskStatus` 目标枚举：`pending/running/completed/failed/timeout/cancelled`。
  - 后端模型仍使用 `processing`、`ready`。
  - 017 迁移存在 `running -> processing`、`completed -> ready` 的反向映射。
- 根因：目标契约与数据库模型迁移目标不一致，历史兼容逻辑和目标语义混杂。
- 复现条件：
  - 执行迁移后检查数据库状态分布，或调用文献/知识库状态接口比对前端状态渲染。
- 风险：
  - 同一业务状态在不同层表达不一致，导致状态机语义漂移和维护误判。

### P1-02 错误响应结构不统一，前端解析路径单一
- 风险等级：`P1`
- 影响范围：后端错误语义统一、前端异常展示与告警定位
- 文件与行号：
  - `backend/app/core/errors.py:12`
  - `backend/app/core/errors.py:19`
  - `backend/app/core/errors.py:21`
  - `backend/app/core/error_handlers.py:20`
  - `backend/app/api/admin.py:73`
  - `backend/app/api/auth.py:29`
  - `backend/app/api/codelab.py:832`
  - `backend/app/api/codelab.py:1054`
  - `frontend/src/services/api.ts:74`
  - `frontend/src/services/api.ts:79`
  - `frontend/src/services/api.ts:80`
- 证据：
  - 既有 `detail: "string"`，也有 `detail: {code,message}`，也有顶层 `AppServiceError` 返回 `{code,message,extra}`。
  - 前端主要读取 `error.response?.data?.detail`，无法统一消费顶层错误结构。
- 根因：未建立统一错误响应 DTO 与全局异常转换层，历史路由直接抛错格式各异。
- 复现条件：
  - 分别触发 admin/auth/codelab 错误分支，比较响应体结构差异。
- 风险：
  - 用户侧错误提示不稳定，排障链路信息丢失，前后端协作成本高。

### P1-03 E2E 对限流敏感，脚本缺少 429 退避策略
- 风险等级：`P1`
- 影响范围：本地与 CI 稳定性、误报失败率
- 文件与行号：
  - `e2e/role_route_smoke_e2e.ps1:82`
  - `e2e/role_route_smoke_e2e.ps1:86`
  - `e2e/remaining_modules_api_smoke_e2e.ps1:59`
  - `e2e/remaining_modules_api_smoke_e2e.ps1:70`
  - `backend/app/config.py:188`
  - `backend/app/config.py:189`
  - `backend/app/config.py:190`
  - `backend/app/config.py:191`
  - `backend/app/config.py:192`
  - `.env.example:145`
  - `.github/workflows/nightly-e2e-full.yml:22`
  - `.github/workflows/nightly-e2e-full.yml:24`
- 证据：
  - 首轮并发运行 mentor/admin 路由脚本返回 429：`retry_after_seconds=36`。
  - 等待并串行重跑后通过，表明业务功能可用，脚本鲁棒性不足。
  - CI 使用更高限流阈值（200/240/480/120），本地默认更低（20/60/120/30）。
- 根因：测试请求封装未做统一重试退避，且本地/CI 限流策略不一致。
- 复现条件：
  - 并发启动两条 role route 脚本或连续高频运行 E2E。
- 风险：
  - 非业务失败被误判为功能回归，发布门禁稳定性下降。

### P1-04 后端全集测试在宿主环境不可复现
- 风险等级：`P1`
- 影响范围：回归验证、故障定位速度、交付可信度
- 文件与行号：
  - `docs/TESTING_GUIDE.md:26`
  - `docs/TESTING_GUIDE.md:35`
  - `backend/requirements.txt`
- 证据：
  - `python -m pytest backend/tests -q` collection 阶段 39 errors。
  - 典型报错：`No module named fastapi/loguru/sqlalchemy/pydantic_settings`。
- 根因：缺少“单命令、可复制”的宿主环境测试准备流程与依赖校验门禁。
- 复现条件：
  - 在未安装完整依赖的宿主 Python 环境直接执行后端全集 pytest。
- 风险：
  - 代码质量检查对环境偶然性强，导致“本地不可测、线上再发现”。

### P1-05 合约门禁覆盖不足（仅覆盖前端与部署配置）
- 风险等级：`P1`
- 影响范围：契约漂移防护能力
- 文件与行号：
  - `backend/checks/check_contract_alignment.py:8`
  - `backend/checks/check_contract_alignment.py:9`
  - `backend/checks/check_contract_alignment.py:10`
  - `backend/checks/check_contract_alignment.py:119`
  - `backend/checks/check_contract_alignment.py:140`
  - `backend/checks/check_contract_alignment.py:146`
- 证据：
  - 当前脚本仅校验 `frontend/src/services/api.ts`、`docker-compose.yml`、`.env.example`。
  - 未校验 `backend/app/models/*` 与 `backend/alembic/versions/*` 的状态枚举语义一致性。
- 根因：契约校验聚焦前端类型与部署默认值，缺少后端模型层比对。
- 复现条件：
  - 修改后端状态枚举但不动前端类型，`check_contract_alignment.py` 仍可通过。
- 风险：
  - 契约“看似通过”但运行时语义失配，问题进入集成期才暴露。

## 3. P2（中优先级）

### P2-01 Broad Exception 门禁存在盲区
- 风险等级：`P2`
- 影响范围：异常治理可信度
- 文件与行号：
  - `backend/checks/check_no_new_broad_excepts.py:7`
  - `backend/checks/check_no_new_broad_excepts.py:19`
  - `backend/app/api/codelab.py:198`
  - `backend/app/api/codelab.py:209`
  - `backend/app/api/codelab.py:289`
  - `backend/app/api/codelab.py:396`
- 证据：
  - 门禁仅统计 `except Exception`，不统计 `except:` 裸异常。
  - 代码中仍存在多处 `except:`，当前门禁无法覆盖。
- 根因：静态规则定义过窄（关键字匹配 + 白名单基线）。
- 复现条件：
  - 新增 `except:` 到非白名单或白名单文件，门禁可能不报。
- 风险：
  - 异常吞噬点漏检，回归时难以发现根因。

### P2-02 CodeLab 全局缓存为进程内状态，缺少跨进程一致性保障
- 风险等级：`P2`
- 影响范围：Notebook 列表/详情读取一致性
- 文件与行号：
  - `backend/app/api/codelab.py:544`
  - `backend/app/api/codelab.py:547`
  - `backend/app/api/codelab.py:583`
  - `backend/app/api/codelab.py:595`
  - `backend/app/api/codelab.py:608`
  - `backend/app/api/codelab.py:869`
- 证据：
  - `_notebooks_cache`、`_loaded_users` 为全局内存对象，读写分散且缺少跨 worker 协调。
- 根因：以单进程缓存换取响应速度，但未定义多实例一致性策略。
- 复现条件：
  - 多 worker 部署下并发读写同一 notebook，观察列表与详情偶发不一致。
- 风险：
  - 状态陈旧、脏读、调试困难。

### P2-03 事务边界语义分散（依赖层自动 commit + 业务层显式 commit）
- 风险等级：`P2`
- 影响范围：事务一致性与可维护性
- 文件与行号：
  - `backend/app/core/database.py:60`
  - `backend/app/core/database.py:65`
  - `backend/app/core/database.py:67`
  - `backend/app/api/knowledge.py:479`
  - `backend/app/api/knowledge.py:529`
  - `backend/app/api/knowledge.py:1138`
  - `backend/app/api/knowledge.py:1251`
- 证据：
  - 依赖层 `get_db` 在请求结束后自动 `commit/rollback`。
  - 业务路由内部又有大量 `await db.commit()` 与 `await db.rollback()`。
- 根因：事务控制职责未统一（请求级与用例级混用）。
- 复现条件：
  - 在复杂分支中插入异常，检查部分写入是否按预期回滚。
- 风险：
  - 事务语义难推理，后续重构容易引入副作用。

### P2-04 前端分包策略失效与大 chunk 风险
- 风险等级：`P2`
- 影响范围：首页加载性能、弱网体验
- 文件与行号：
  - `frontend/src/components/layout/MainLayout.tsx:36`
  - `frontend/src/components/layout/MainLayout.tsx:257`
- 证据：
  - `npm run build` 告警：`api.ts` 动态引入同时被大量静态引入，动态拆包无效。
  - 存在 >500kB chunk 警告。
- 根因：核心 API 模块被广泛静态引用，同时在局部做动态 import。
- 复现条件：
  - 执行 `npm run build`，检查 reporter 告警。
- 风险：
  - 首屏/路由切换性能下降，包体持续膨胀。

### P2-05 前端运行期告警累积（AntD/Router 升级风险）
- 风险等级：`P2`
- 影响范围：未来升级兼容性、日志噪声
- 文件与行号：
  - `.playwright-cli/console-2026-02-23T12-08-15-976Z.log:3`
  - `.playwright-cli/console-2026-02-23T12-08-15-976Z.log:16`
  - `.playwright-cli/console-2026-02-23T12-08-15-976Z.log:1`
  - `frontend/src/pages/user/SettingsPage.tsx:270`
  - `frontend/src/pages/mentor/AnnouncementsPage.tsx:393`
- 证据：
  - `[antd: message] Static function can not consume context`
  - `[antd: Select] dropdownStyle is deprecated`
  - React Router v7 future flag 警告高频出现
- 根因：历史 API 用法未按新版本推荐迁移。
- 复现条件：
  - 执行 UI E2E（如 role route / settings）并查看 Playwright console log。
- 风险：
  - 框架升级时可能从 warning 变成行为变化或错误。

### P2-06 安全基线改进项：容器默认 root 与弱密码策略
- 风险等级：`P2`
- 影响范围：容器逃逸后横向风险、账号安全强度
- 文件与行号：
  - `backend/Dockerfile:1`
  - `backend/Dockerfile:45`
  - `backend/app/schemas/user.py:19`
  - `backend/app/schemas/user.py:42`
  - `backend/app/schemas/user.py:43`
- 证据：
  - Dockerfile 未设置 `USER`，容器默认 root 运行。
  - 密码最小长度仅 6，缺少复杂度约束。
- 根因：安全缺省值未收敛为“secure-by-default”。
- 复现条件：
  - 审核 Dockerfile 与用户 schema 即可复现。
- 风险：
  - 安全基线偏弱，在攻击面扩大时风险上升。

### P2-07 MCP 探测失败可见但缺少更明确的健康分级
- 风险等级：`P2`
- 影响范围：运维可观测性、故障定位效率
- 文件与行号：
  - `backend/app/services/mcp/client.py:94`
  - `docs/test/全面审查_动态测试证据_2026-02-23_20-13.md`
- 证据：
  - backend 日志出现 `discover failed on server=filesystem`，同时 refresh API 200。
- 根因：接口可用性与子组件可用性未做分级健康表达。
- 复现条件：
  - 触发 MCP 刷新并检查 backend 日志。
- 风险：
  - 外层接口“看起来健康”，但内部能力退化不易被监控告警识别。

## 4. P3（低优先级）

### P3-01 文档一致性偏差（端口与内容分叉）
- 风险等级：`P3`
- 影响范围：新成员接入与运维文档可信度
- 文件与行号：
  - `docs/CONFIGURATION.md:158`
  - `docs/CONFIGURATION.md:216`
  - `docs/TESTING_GUIDE.md:57`
  - `docker-compose.yml:140`
  - `README.md:53`
- 证据：
  - 文档仍有 `localhost:8000` 示例，当前 compose 对外端口为 `8888`。
  - `docs/CONFIGURATION.md` 与 `docs/CONFIGURATIONS.md` 内容分叉，维护成本高。
- 根因：文档缺少单一事实源与变更联动检查。
- 复现条件：
  - 按 `docs/TESTING_GUIDE.md` 的 `8000` 健康检查执行将与当前部署默认不一致。
- 风险：
  - 文档驱动操作失败，增加排障时间。

## 5. 关键复现命令索引
- 后端门禁：
  - `python backend/checks/check_no_new_broad_excepts.py`
  - `python backend/checks/check_contract_alignment.py`
- 后端全集：
  - `python -m pytest backend/tests -q`
- 前端质量：
  - `npm run lint`
  - `npm run build`
- 全链路：
  - `./acceptance_tests.ps1`
  - `./e2e/role_business_api_smoke_e2e.ps1`
  - `./e2e/remaining_modules_api_smoke_e2e.ps1`
  - `./e2e/student_role_smoke_e2e.ps1`
  - `./e2e/role_route_smoke_e2e.ps1 -Role mentor`
  - `./e2e/role_route_smoke_e2e.ps1 -Role admin`
  - `./e2e/mcp_settings_login_e2e.ps1`
  - `./e2e/knowledge_upload_pipeline_e2e.ps1`

