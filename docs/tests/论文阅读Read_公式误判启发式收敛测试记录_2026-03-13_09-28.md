# 论文阅读 Read 公式误判启发式收敛测试记录

时间：2026-03-13 09:28

## 目标

验证 fallback 里对量化配置列表的公式误判被收住，同时不破坏真实公式识别。

## 计划中的验证

1. 单测：
   - 量化配置列表不再判成 `equation`
   - 真实公式文本仍然判成 `equation`
2. 运行态：
   - `paper 85 / page 8` 不再出现错误的 `EquationBlock`

## 风险

- 公式启发式收窄后，少数没有 `layout_type=formula` 且只靠文本识别的真实公式可能会漏判
- 本轮不触碰全局 preview 主链，避免把范围放大

## 已执行验证

1. 单测
   - `./backend/.venv-incremental/bin/python -m pytest backend/tests/test_literature_reader_composed.py -k "detect_table_caption_and_equation or quantization_config_lists_as_equations" -q`
   - 结果：`2 passed`

2. 语法检查
   - `python3 -m py_compile backend/app/services/literature_reader_compose_service.py`
   - 结果：通过

3. Live fresh rebuild
   - 在 backend 容器内对 `paper 85 / page 8 / kb 84` 执行 `force_refresh=True, regenerate=True`
   - 返回：
     - `status = fallback`
     - `engine_version = reader_compose_v13`
     - `build_mode = compose_agent_layout_uid_v1`
   - 组件类型中，原本被误判成公式的编号/项目配置项已变成 `ListBlock`
