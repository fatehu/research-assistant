# 论文阅读 Read Table 证据与公式渲染测试记录

时间：2026-03-12 16:26

## 目标

验证本轮两类修复：

1. `/read` 表格 evidence 不再偏移，并恢复显式证据入口
2. `/read` 公式块不再直接显示错乱字符串，而是走 KaTeX + 原图证据

## 自动化验证

### Backend

命令：

```bash
backend/.venv-incremental/bin/python -m pytest backend/tests/test_literature_reader_composed.py -k "materialize_table_and_equation or equation_props_should_split_where_clause_into_description or preserve_docmind_table_cells_in_page_grounding or layout_uid or page_grounding_v1 or doi_layout_outside_main_flow" -q
backend/.venv-incremental/bin/python -m pytest backend/tests/test_literature_reader_api.py -k "default_to_layout_uid_v1_pipeline or pipeline_version_override or repair_malformed_fallback_payload" -q
backend/.venv-incremental/bin/python backend/checks/check_contract_alignment.py
backend/.venv-incremental/bin/python backend/checks/check_no_new_broad_excepts.py
```

结果：

- `test_literature_reader_composed.py`：`11 passed`
- `test_literature_reader_api.py`：`3 passed`
- `check_contract_alignment.py`：passed
- `check_no_new_broad_excepts.py`：passed

### Frontend

命令：

```bash
cd frontend && timeout 120 ./node_modules/.bin/eslint src/pages/literature/readerComponents/index.tsx src/pages/literature/readerComponents/schemas.ts --format unix
cd frontend && timeout 180 ./node_modules/.bin/tsc --noEmit --pretty false
```

结果：

- 定向 eslint：`EXIT:0`
- `tsc --noEmit`：`EXIT:0`

## 关键断言

### 表格

- `TablePanel.props.table_cells` 被保留，不再在 contract normalize 过程中丢失
- `row_evidence.anchor.geometry.page_width/page_height` 为 `None`
- `row_evidence.anchor.bbox_hint.page_width/page_height` 为 `None`
- `TablePanel` 继续保留 `row_evidence`

### 公式

- `_build_layout_uid_equation_props(...)` 会把 trailing `where ...` 拆到 `description`
- 公式编号 `(1)` 可拆到 `label`
- 前端 `EquationBlockNode` 改为：
  - 先显示 evidence image
  - 再做 KaTeX 渲染

## 手动验收地址

- 表格页：
  - `http://localhost:3000/literature/85/read?page=7&kb=84`
- 公式页：
  - `http://localhost:3000/literature/85/read?page=3&kb=84`

## 手动验收点

### 表格页

- 顶部是否恢复显式 `证据 / 预览`
- 点击整表 `证据` 时是否回到整表定位
- 悬停某一行时 evidence image 是否贴近对应行，而不是偏到整表其它位置
- 表格是否优先按 `table_cells` 呈现，而不是一维 markdown 串
- 顶部不要再把完整 caption 重复成 card title，caption 只保留在表格下方
- 数值列应自动居中，整体样式更接近学术表而不是满格 spreadsheet

### 公式页

- 是否先看到原公式证据图
- 公式正文是否经过 KaTeX 渲染，而不是单纯原始字符串
- trailing `where ...` 是否变成解释文本，而不是和公式主体混在一起

## 已知剩余问题

- 复杂 `rowspan / colspan` 表格仍需下一轮继续做更高保真重建
- 公式 OCR 如果本身极差，KaTeX 仍可能只能部分改善；当前通过“原图证据 + 渲染文本”双轨兜底
