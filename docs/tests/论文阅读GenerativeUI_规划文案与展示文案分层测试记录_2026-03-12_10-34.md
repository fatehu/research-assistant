# 论文阅读 Generative UI：规划文案与展示文案分层测试记录

## 范围

- 把 storyboard planning copy 从 `/experience` 主视图移出
- 保留 planner purpose 到 section metadata
- 修正 hero mini card 语义标签

## 自动化验证

### Backend

```bash
backend/.venv-incremental/bin/python -m pytest backend/tests/test_generative_reader_agent_runtime.py -q
```

结果：

- `33 passed`

新增回归覆盖：

- `test_build_experience_plan_should_keep_storyboard_purpose_out_of_user_visible_summary`

验证内容：

- `planner_purpose` 会保留在 `section.meta`
- `display_summary` 不再直接等于 storyboard `purpose`
- 页面不再直接暴露“理解抓手 / 清洗后的正文 / 辅助卡片替代正文”这类 planning 腔

```bash
backend/.venv-incremental/bin/python -m pytest backend/tests/test_generative_ui_evaluation.py -q
```

结果：

- `3 passed`

```bash
backend/.venv-incremental/bin/python -m pytest backend/tests/test_literature_reader_api.py -k "experience_plan or generative_plan or reader_experience_flow" -q
```

结果：

- `7 passed`

```bash
backend/.venv-incremental/bin/python backend/checks/check_contract_alignment.py
```

结果：

- `Contract alignment guard passed.`

### Frontend

```bash
cd frontend && ./node_modules/.bin/eslint src/pages/literature/GenerativeExperienceRenderer.tsx
```

结果：

- 退出码 `0`

## 本轮确认点

- hero mini card 的标签从“为什么值得读”修正为“阅读切入点”
- planning note 继续留在 debug 元数据中
- `/experience` 主视图不再默认展示 storyboard 内部 purpose
- experience cache key 已升级，避免旧的 planning copy page summary 继续命中缓存

## 已知限制

- 本轮主要解决的是 section summary 的 planning 腔暴露
- header hooks、resource strategy 等其他展示字段后续仍需要继续做语义分层审查
