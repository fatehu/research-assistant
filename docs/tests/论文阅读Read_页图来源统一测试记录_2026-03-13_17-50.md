# 论文阅读 Read 页图来源统一测试记录

时间：2026-03-13 17:50

## 目标

验证 `/read` 的 AI prompt 图和 grounding/page_image 图统一到本地 PDF render asset。

## 计划验证

1. 单测
   - 已有 page render asset 时，优先返回 render asset 的 `url/path/source`
   - 没有 render asset 时，才回退到 DocMind URL 本地化

2. 运行态
   - fresh rebuild 后，`page_grounding_v1.page_image.path` 指向本地 render asset
   - `single_agent_v2` prompt 不再优先依赖 DocMind 临时 URL

## 已执行验证

### 代码层

1. `python3 -m py_compile backend/app/services/literature_reader_compose_service.py`
   - 通过

2. `./backend/.venv-incremental/bin/python -m pytest backend/tests/test_literature_reader_composed.py -k "localize_remote_prompt_image_for_dashscope or localize_remote_page_image_when_path_missing or prefer_existing_render_asset or quantization_config_lists_as_equations" -q`
   - 结果：`4 passed`

3. `./backend/.venv-incremental/bin/python backend/checks/check_contract_alignment.py`
   - 通过

### 运行态

1. backend 健康检查
   - `http://localhost:8888/docs` -> `200`

2. 当前运行容器 SDK 状态
   - `reader_agent_provider = aliyun`
   - `DashScopeMultimodalService.is_available() = True`

3. live fresh rebuild
   - 目标：`paper 85 / page 8 / kb 84 / layout_uid_v1`
   - 结果：
     - `engine_version = reader_compose_v14`
     - `status = fallback`
     - `build_mode = compose_agent_layout_uid_v1`
     - `page_grounding_v1.page_image.source = page_render_asset`
     - `page_grounding_v1.page_image.path = /app/uploads/reader_page_assets/85/page_8.jpg`
     - `qwen_plan_meta.total_tokens = 16431`
   - provider 日志确认：
     - `DashScopeMultimodalService multimodal call finished ... images=1`

### 说明

当前这轮已经证明：

- 图片链统一成功
- live 请求真实消费了本地 `page_8.jpg`
- page 8 仍然 fallback，但原因已经不是“图片送不进去”

## 风险

- 不允许把 `page_image.width/height` 直接切到 PDF render asset 尺寸
- 否则可能再次破坏高光坐标基准

## 容器说明

本次运行态修复还补了一步即时处理：

- 由于当前 `research_backend` 容器镜像里缺少 `dashscope`
- 已在运行容器中执行：
  - `pip install dashscope==1.24.2`

仓库的 `backend/requirements.txt` 原本已包含该依赖；后续重新完整构建 backend 镜像后，这一步会自然固化到镜像中。
