# 论文阅读 `/read` 撤销全局 Raster 预览特判测试记录

## 目的

确认 `/read` 的 evidence preview 已从错误的 `layout_uid_v1 -> raster` 全局特判回退到统一的 PDF.js 预览主链。

## 执行命令

```bash
npm --prefix frontend run lint
cd frontend
npx tsc --noEmit
```

## 结果

- `npm --prefix frontend run lint`
  - passed
- `cd frontend && npx tsc --noEmit`
  - passed

## 人工验收点

直接访问：

- `http://localhost:3000/literature/85/read?page=7&kb=84`
- `http://localhost:3000/literature/85/read?page=3&kb=84`

操作：

1. 硬刷新
2. hover 一个正文或表格节点
3. 再点一次 `证据`

预期：

- 不再出现统一的右下偏移
- hover evidence 与 pinned evidence 使用同一条 PDF.js 预览逻辑
- 表格问题如果还存在，也应只表现为表格自身 fidelity，不再污染所有节点的 evidence preview
