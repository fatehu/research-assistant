# 论文阅读 Read 恢复整表证据链与 DocMind 高光测试记录

时间：2026-03-12 18:55

## 本轮目标

恢复 `/read` 在表格重建前的稳定证据链：

- 表格 hover 回到整表
- 菜单中的 `证据` 回到通用节点行为
- 高光重新围绕 `uniqueId -> blocks[].pos`

## 代码变更

- `backend/app/services/literature_reader_compose_service.py`
  - 去掉 `table` 物化时只保留表体 `source_layout_ids` 的特判
  - `TablePanel` 再次保留完整 table bundle 作为证据源
- `frontend/src/pages/literature/readerComponents/index.tsx`
  - 去掉 `TablePanel` 的 row/cell hover/click evidence 逻辑
  - 恢复通用 `ActionBar` 证据入口
  - 保留表格展示与导出能力
- `backend/tests/test_literature_reader_composed.py`
  - 更新 table bundle 物化断言

## 自动化验证

1. 后端定向回归

命令：

```bash
set -a && source <(tr -d '\r' < .env) && set +a && cd backend && ./.venv-incremental/bin/python -m pytest tests/test_literature_reader_composed.py -k "layout_uid_group_plan_to_panel_plan_should_materialize_table_and_equation or no_drop_fallback or page_grounding_v1 or table_cells" -q
```

结果：

- `5 passed`

2. 前端定向 ESLint

命令：

```bash
cd frontend && npx eslint src/pages/literature/readerComponents/index.tsx --max-warnings=0
```

结果：

- `EXIT:0`

3. 前端 TypeScript

命令：

```bash
cd frontend && timeout 120s npx tsc --noEmit; echo EXIT:$?
```

结果：

- `EXIT:0`

4. 仓库保护检查

命令：

```bash
./backend/.venv-incremental/bin/python backend/checks/check_contract_alignment.py
./backend/.venv-incremental/bin/python backend/checks/check_no_new_broad_excepts.py
```

结果：

- `Contract alignment guard passed.`
- `Broad exception guard passed.`

## 运行态说明

- `COMPOSE_ENGINE_VERSION` 已提升到 `reader_compose_v8`
- 目的：避免继续命中旧的表格重建缓存结果

## 手动验收建议

先硬刷新，再重新生成：

- `http://localhost:3000/literature/85/read?page=7&kb=84`
- `http://localhost:3000/literature/85/read?page=3&kb=84`

重点检查：

- `...` 菜单里是否重新稳定出现 `证据`
- hover 整张表时是否显示整表 evidence，而不是行/单元格碎片
- pinned evidence 是否重新按整组 uniqueId 对齐
- 公式页面是否仍保持证据可见
