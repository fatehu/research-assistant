## 目标

验证 `/read` 正常页面访问时，表格 AI logical-row 重建不再因为远程页图 URL 而 fallback。

## 预期验证

1. `rendered_page_image_path` 为空时，可从安全远程 URL 落成本地缓存图片
2. `_invoke_single_agent_model(...)` 能拿到本地 `image_paths`
3. live logs 不再出现：
   - `Failed to download multimodal content`

## 本轮只验证

- 远程页图 -> 本地缓存 这一步
- 不要求这一轮同时收完表格视觉 fidelity

## 自动化结果

- `python3 -m py_compile backend/app/services/literature_reader_compose_service.py backend/tests/test_literature_reader_composed.py`
  - 通过
- `./backend/.venv-incremental/bin/python -m pytest backend/tests/test_literature_reader_composed.py -k "localize_remote_page_image or localize_remote_prompt_image or keep_ai_table_logical_row_fields or pairing_hints" -q`
  - `4 passed`
- `./backend/.venv-incremental/bin/python backend/checks/check_contract_alignment.py`
  - 通过
- `./backend/.venv-incremental/bin/python backend/checks/check_no_new_broad_excepts.py`
  - 通过

## 运行态验证

- `docker compose restart backend frontend`
  - 两个容器均恢复 `Up`
- fresh 请求：
  - `POST /api/v1/literature/papers/85/reader/composed`
  - body: `{"page":7,"selected_kb_id":84,"force_refresh":true,"regenerate":true}`
  - 返回 `200`
- backend 日志确认：
  - `single_agent_v2 pipeline done paper=85 page=7 build_mode=compose_agent_layout_uid_v1`
  - 未再出现 `Failed to download multimodal content`
- cached payload 关键字段：
  - `engine_version = reader_compose_v11`
  - `page_grounding_v1.page_image.path = /app/uploads/reader_page_assets/paper_85/grounding_pages/page_7.png`
  - `TablePanel.props.reconstruction_mode = ai_logical_rows`
  - `logical_rows = 13`
