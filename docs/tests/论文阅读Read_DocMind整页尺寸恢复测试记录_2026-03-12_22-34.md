# 论文阅读 `/read` DocMind 整页尺寸恢复测试记录

## 目的

确认 `layout_uid_v1` anchor 不再使用“内容边界”作为整页缩放基准，而是回到真实 DocMind 页图尺寸。

## 自动化验证

执行：

```bash
./backend/.venv-incremental/bin/python -m pytest backend/tests/test_literature_reader_composed.py -k "page_grounding_image_dimensions or page_grounding_v1 or stale_engine_version or compatible_db_cache_before_rebuild or ignore_other_pipeline_version" -q
./backend/.venv-incremental/bin/python backend/checks/check_contract_alignment.py
./backend/.venv-incremental/bin/python backend/checks/check_no_new_broad_excepts.py
```

结果：

- `pytest ...` -> `5 passed`
- `check_contract_alignment.py` -> passed
- `check_no_new_broad_excepts.py` -> passed

## 运行态验证

1. 重启 backend
2. 触发一次：

```bash
POST /api/v1/literature/papers/85/reader/composed/stream
{
  "page": 3,
  "selected_kb_id": 84,
  "pipeline_version": "layout_uid_v1"
}
```

3. 再读取：

```bash
POST /api/v1/literature/papers/85/reader/composed/cached
{
  "page": 3,
  "selected_kb_id": 84,
  "pipeline_version": "layout_uid_v1"
}
```

确认：

- `engine_version = reader_compose_v9`
- `geometry.page_width/page_height = 1483 x 1920`
- `bbox_hint.page_width/page_height = 1483 x 1920`

## 手工验收地址

- `http://localhost:3000/literature/85/read?page=3&kb=84`
- `http://localhost:3000/literature/85/read?page=7&kb=84`

操作：

1. 硬刷新
2. 点一次“重新生成”
3. hover 正文或点 `证据`

预期：

- 不再出现统一右下偏移
- pinned evidence 与 hover evidence 对齐
- 表格 fidelity 问题如果还有，也不应再表现成整页缩放偏移
