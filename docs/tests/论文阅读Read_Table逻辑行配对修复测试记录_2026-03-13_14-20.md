## 目标

验证 `/read` 表格在 `ai_logical_rows` 已真实接入的前提下，进一步把：

- 稀疏 value row
- uncertainty / continuation row

合并成更接近原论文的逻辑行。

## 这轮重点验证

1. `paper 85 / page 6` 里 `(±...)` 不再和上一数值行拆开
2. `source_row_indices` 会在合并后保留完整来源
3. 不破坏现有 `page 7` 的 benchmark 表
4. 不触碰全局 evidence preview 主链

## 待记录

- 定向 pytest
- lint / typecheck
- `paper 85 / page 6` live payload 中 `logical_rows` 的前几行对比
- 页面验收结论

## 已记录

- `./backend/.venv-incremental/bin/python -m pytest backend/tests/test_literature_reader_composed.py -k "merge_sparse_value_row_with_uncertainty_row or merge_blank_lead_uncertainty_row_into_previous or localize_remote_prompt_image or pairing_hints" -q`
  - `4 passed`
- fresh payload 实测：
  - `paper 85 / page 6`
  - `reconstruction_mode = ai_logical_rows`
  - `logical_rows_len = 13`
  - `AIME 2024 -> 38.34\\n(±2.52)`
  - `MATH 500 -> 89.85\\n(±0.30)`
- 当前补充动作：
  - `COMPOSE_ENGINE_VERSION` 从 `reader_compose_v11` 提升到 `reader_compose_v12`
  - 目的不是改逻辑，而是切断旧缓存复用，确保页面命中新物化结果
