# 论文阅读 `/read` 表格重构回归与证据链恢复测试记录

## 目的

确认这次恢复覆盖了两个真正的运行态问题：

1. 旧 `reader_compose_v7/v6` payload 不再继续复用
2. compatible cache 不再跨 pipeline version 污染 `layout_uid_v1`

## 执行命令

```bash
set -a && source <(tr -d '\r' < .env) && set +a
cd backend
./.venv-incremental/bin/python -m pytest tests/test_literature_reader_composed.py -k "stale_engine_version or compatible_db_cache_before_rebuild or ignore_other_pipeline_version" -q
cd ..
./backend/.venv-incremental/bin/python backend/checks/check_contract_alignment.py
./backend/.venv-incremental/bin/python backend/checks/check_no_new_broad_excepts.py
docker compose restart backend
```

## 结果

- `pytest ... -k "stale_engine_version or compatible_db_cache_before_rebuild or ignore_other_pipeline_version" -q`
  - `3 passed`
- `check_contract_alignment.py`
  - passed
- `check_no_new_broad_excepts.py`
  - passed
- backend restart
  - `http://localhost:8888/docs -> 200`

## 备注

这次记录的重点是 cache 行为与证据链恢复，不是表格 fidelity 本身。
