# 全量整改 阶段11 测试记录：端到端流程打通

- 时间：2026-02-23 11:53
- 测试人：Codex
- 分支：`feature/full-remediation-2026-02`

## 环境

- 操作系统：Windows（PowerShell）
- Docker：Desktop Linux Engine（本轮手动拉起后可用）
- 前端地址：`http://localhost:3000`
- 后端地址：`http://localhost:8888`

## 关键阻塞与修复

1. 阻塞：容器启动初期 `docker compose up -d --build ...` 在 `apt-get` 阶段 OOM（exit 137）。
- 处置：使用已有镜像启动全栈（不强制重建）完成本轮 E2E。

2. 阻塞：`remaining_modules_api_smoke_e2e.ps1` 文档处理阶段失败，错误为本地嵌入加载 `RetryError`。
- 根因：容器访问 `huggingface.co` 发生 TLS EOF。
- 处置：后端注入 `HF_ENDPOINT=https://hf-mirror.com`，并重建 backend 容器。

## 执行命令与结果

1. `./acceptance_tests.ps1`
- 结果：通过

2. `./e2e/role_business_api_smoke_e2e.ps1`
- 结果：通过

3. `./e2e/remaining_modules_api_smoke_e2e.ps1`
- 结果：通过（修复 `HF_ENDPOINT` 后回归通过）

4. `./e2e/student_role_smoke_e2e.ps1`
- 结果：通过

5. `./e2e/role_route_smoke_e2e.ps1 -Role mentor`
- 结果：通过

6. `./e2e/role_route_smoke_e2e.ps1 -Role admin`
- 结果：通过

7. `./e2e/mcp_settings_login_e2e.ps1`
- 结果：通过

8. `./e2e/knowledge_upload_pipeline_e2e.ps1`
- 结果：通过

9. `docker compose ps`
- 结果：通过（`backend/frontend/postgres/redis/codelab-runner` 均 Up，数据库与 Redis healthy）

## 产物

- Playwright 截图输出：`output/playwright/*.png`
- Playwright 快照/控制台：`.playwright-cli/*`

## 结论

- 本轮“涉及到的全部流程”已完成端到端打通。
- 当前环境可复现执行上述全套 E2E 脚本并得到通过结果。
