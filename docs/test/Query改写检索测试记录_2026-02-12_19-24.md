# Query 改写检索测试记录（2026-02-12）

- 记录时间：2026-02-12 19:24
- 测试人：Codex

## 1. 变更目标
- 在检索前引入 Query Rewrite（同义扩展 / HyDE / 子问题分解）。
- 验证与现有 `混合检索 + RRF + Reranker` 的兼容性。
- 验证失败降级（改写失败时回退原始 query）。

## 2. 测试环境
- 项目路径：`D:\Codefield\agent-platform\research-assistant`
- 执行方式：Docker Compose（`research_backend` 容器）
- 时间窗口：2026-02-12 19:21 ~ 2026-02-12 19:24
- 关键服务状态：
  - `research_backend`：Up
  - `research_postgres`：healthy
  - `research_redis`：healthy

## 3. 测试项与结果

### 3.1 语法检查
- 命令：
```bash
docker compose exec -T backend python -m py_compile app/services/query_rewrite_service.py app/services/hybrid_retrieval_service.py app/api/knowledge.py app/services/agent_tools.py app/config.py app/schemas/knowledge.py tests/test_hybrid_retrieval_service.py tests/test_query_rewrite_service.py
```
- 结果：通过

### 3.2 单元测试
- 命令：
```bash
docker compose exec -T backend pytest tests/test_hybrid_retrieval_service.py tests/test_query_rewrite_service.py -q
```
- 结果：通过（`9 passed`）
- 备注：出现 `pytest-asyncio` 的 `event_loop` 重定义警告（已有测试基线问题，不影响本次功能正确性）。

### 3.3 本地环境回归说明
- 命令：
```bash
pytest backend/tests/test_hybrid_retrieval_service.py backend/tests/test_query_rewrite_service.py -q
```
- 结果：失败（本机缺少 `pydantic_settings` 依赖）。
- 处理：切换到 Docker 后端容器执行，验证通过。

## 4. 核心验证点
- `query_rewrite_service`：
  - 策略别名解析正常；
  - fenced JSON 可解析；
  - 关闭开关与 LLM 异常能降级到原始 query。
- 多查询召回聚合：
  - 同一 chunk 多路命中时保留最高分；
  - 保留命中的改写 query/strategy 追踪信息。
- 检索融合：
  - 与 `fuse_rrf` 流程兼容，未破坏原有排序逻辑。

## 5. 结论
- 本次 Query Rewrite 功能在后端容器环境验证通过。
- 核心检索链路（API 与 Agent 工具）已具备三种改写策略与降级能力。
- 当前无阻断上线问题。
