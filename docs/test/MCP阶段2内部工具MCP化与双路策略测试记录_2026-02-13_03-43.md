# MCP阶段2内部工具MCP化与双路策略测试记录（2026-02-13 03:43）
- 记录时间：2026-02-13 03:43
- 测试人：Codex
- 环境：Windows + PowerShell + `.venv`

## 1. 测试目标
1. 验证 `web_search` 双路策略：外部成功时不走本地。
2. 验证外部失败自动回退本地逻辑。
3. 验证重试/熔断策略行为。
4. 验证内部 `web/literature` MCP 服务封装行为。

## 2. 执行命令与结果
### 2.1 MCP 相关单测
命令：
```bash
.venv\Scripts\python.exe -m pytest backend/tests/test_tool_registry_knowledge_search.py backend/tests/test_tool_registry_mcp_bridge.py backend/tests/test_tool_registry_mcp_routing.py backend/tests/test_mcp_config.py backend/tests/test_internal_mcp_servers.py -q
```
结果：
1. `14 passed`
2. `0 failed`

### 2.2 ReAct/上下文/数据库回归
命令：
```bash
.venv\Scripts\python.exe -m pytest backend/tests/test_react_agent_citation_policy.py backend/tests/test_agent_db_wiring.py backend/tests/test_contextual_compression_service.py -q
```
结果：
1. `25 passed`
2. `0 failed`
3. `3 warnings`（历史 Pydantic deprecation warning，本次未新增）。

### 2.3 检索链路回归
命令：
```bash
.venv\Scripts\python.exe -m pytest backend/tests/test_hybrid_retrieval_service.py backend/tests/test_query_rewrite_service.py backend/tests/test_vector_search_tuning.py -q
```
结果：
1. `12 passed`
2. `0 failed`

### 2.4 语法检查
命令：
```bash
.venv\Scripts\python.exe -m compileall backend/app/mcp_servers backend/app/services/agent_tools.py backend/app/config.py -q
```
结果：通过（无编译错误）。

### 2.5 Docker Compose 配置检查
命令：
```bash
docker compose config --quiet
```
结果：通过（仅存在未设置可选变量告警，不影响配置合法性）。

### 2.6 合并回归（一次性命令）
命令：
```bash
.venv\Scripts\python.exe -m pytest backend/tests/test_tool_registry_knowledge_search.py backend/tests/test_tool_registry_mcp_bridge.py backend/tests/test_tool_registry_mcp_routing.py backend/tests/test_mcp_config.py backend/tests/test_internal_mcp_servers.py backend/tests/test_react_agent_citation_policy.py backend/tests/test_agent_db_wiring.py backend/tests/test_contextual_compression_service.py backend/tests/test_hybrid_retrieval_service.py backend/tests/test_query_rewrite_service.py backend/tests/test_vector_search_tuning.py -q
```
结果：
1. `51 passed`
2. `0 failed`
3. `3 warnings`（历史 Pydantic deprecation warning）。

## 3. 结论
1. 阶段2的内部 MCP 服务封装、双路策略、超时/重试/熔断行为符合预期。
2. 本地回退链路可用，未出现“外部异常导致工具不可用”的回归。
3. 说明：执行过一次 `pytest backend/tests -q`，该命令在当前环境超时，因此以分组回归结果作为本次可复现测试依据。
