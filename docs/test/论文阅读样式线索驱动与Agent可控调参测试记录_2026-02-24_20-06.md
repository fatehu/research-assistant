# 论文阅读样式线索驱动与Agent可控调参测试记录（2026-02-24 20:06）

## 测试环境
- 路径：`d:\Codefield\agent-platform\research-assistant`
- 日期：2026-02-24

## 自动化测试
1. 后端
- 命令：
  - `.venv-ragtest\Scripts\python.exe -m pytest -q backend/tests/test_literature_reader_generative.py backend/tests/test_literature_reader_api.py`
- 结果：`17 passed`

2. 前端
- 命令：
  - `npm run build`（workdir=`frontend`）
- 结果：构建通过

## 新增验证点
1. 噪声文本过滤
- 用例：`test_parse_page_structure_filters_noise_line`
- 结论：`a1111111111` 不再进入结构化正文。

2. style_tuning 边界归一化
- 用例：`test_validate_agent_payload_normalizes_style_tuning`
- 结论：越界值会被钳制到白名单范围。

3. SSE/前端兼容
- 骨架事件新增 `style_tuning`，前端可正常消费并渲染。

## Docker 回归
1. 构建
- 命令：`docker compose up -d --build backend frontend`
- 结果：成功

2. 运行状态
- 命令：`docker compose ps`
- 结果：`backend/frontend/postgres/redis` 均正常

3. 日志检查
- 命令：
  - `docker compose logs --tail 80 backend`
  - `docker compose logs --tail 80 frontend`
- 结果：服务启动与监听正常