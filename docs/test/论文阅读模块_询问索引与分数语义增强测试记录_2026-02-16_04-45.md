# 论文阅读模块：询问索引与分数语义增强测试记录（2026-02-16 04:45）

## 1. 测试环境
- OS: Windows（PowerShell）
- Backend: FastAPI（本地源码）
- Frontend: Vite + React + TypeScript

## 2. 执行命令
1. `python -m compileall backend/app/api/literature.py backend/app/schemas/literature.py backend/app/services/status_event_bus.py`
2. `cd frontend && npm run build`

## 3. 结果
### 3.1 后端语法
- 结果：通过
- 关键输出：
  - `Compiling 'backend/app/api/literature.py'...`
  - `Compiling 'backend/app/schemas/literature.py'...`

### 3.2 前端构建
- 结果：通过
- 关键输出：
  - `tsc -b && vite build`
  - `✓ built in 13.30s`

## 4. 功能性检查点（代码层）
1. 来源序号 `idx`：
- 后端已写入与透传。
- 前端已用于回答 `[n]` 点击映射与来源标题显示。

2. 分数语义：
- fallback 场景 `score=null` + `score_source=fallback`。
- 前端显示 `分数: 无（回退检索）`。

3. 文本模式提取：
- 增加 `pdfjs.getDocument` 预加载，不依赖仅 PDF 可视模式触发。

## 5. 已知风险
- 旧历史会话若无 `idx`，前端按来源顺序回退映射（`1..N`），通常可用但非绝对精确。
- 本阶段未引入 agentic 多轮检索执行引擎，仅完成引用可解释性与可用性修复。

## 6. 结论
- 本阶段改动构建可通过，且覆盖“引用索引缺失”“分数误导”“文本模式初始化空白”三类问题。

