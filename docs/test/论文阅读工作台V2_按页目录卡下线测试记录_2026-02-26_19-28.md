# 论文阅读工作台V2：按页目录卡下线测试记录

- 时间：2026-02-26 19:28
- 环境：Windows + PowerShell + `.venv-ragtest`

## 测试项
1. 语法检查
   - 命令：`python -m py_compile backend/app/services/literature_reader_compose_service.py`
   - 结果：通过
2. 后端回归
   - 命令：`$env:DEBUG='false'; .\.venv-ragtest\Scripts\python.exe -m pytest -q backend/tests/test_literature_reader_composed.py`
   - 结果：通过（21 passed）

## 异常与处理
1. 首次执行 pytest 失败，原因为本地环境变量 `DEBUG=release` 与 bool 配置不兼容。
2. 通过在测试命令前显式设置 `$env:DEBUG='false'` 解决。

## 结论
1. 按页目录卡已下线，构建与回归通过。
2. 后续若需要导航能力，应实现跨页聚合目录而非单页 TOC。
