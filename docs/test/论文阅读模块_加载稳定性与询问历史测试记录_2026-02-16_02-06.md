# 论文阅读模块：加载稳定性与询问历史测试记录（2026-02-16 02:06）

## 1. 测试环境
- OS: Windows (PowerShell)
- Python: `.venv-ragtest`
- Node: `v20.19.4`
- Docker Compose: 本仓库 `docker-compose.yml`

## 2. 执行命令与结果
### 2.1 后端语法检查
```powershell
.venv-ragtest\Scripts\python.exe -m py_compile backend/app/api/literature.py backend/app/schemas/literature.py backend/tests/test_literature_reader_api.py
```
- 结果：通过。

### 2.2 后端新增自动化测试
```powershell
.venv-ragtest\Scripts\python.exe -m pytest backend/tests/test_literature_reader_api.py -q
```
- 结果：`5 passed`。
- 覆盖点：
  - 本地 PDF 读取成功路径
  - 本地 PDF 缺失 404 路径
  - 询问会话列表读取
  - 询问消息 sources 清洗
  - 非法会话 404

### 2.3 前端构建验证
```powershell
cd frontend
npm run build
```
- 结果：构建通过（Vite build success）。

### 2.4 Docker 冒烟（按规范强制 build）
```powershell
docker compose up -d --build backend frontend
docker compose ps
docker compose logs --tail 120 backend
docker compose logs --tail 120 frontend
```
- 结果：
  - `backend/frontend` 容器均正常启动。
  - backend 日志无启动异常。
  - frontend 日志显示依赖安装后成功启动 Vite。

## 3. 结论
- 本次修复覆盖“PDF 加载失败+五项遗留问题”核心路径。
- 本地、构建、容器三个层面的验证均通过，可进入手测验收。

## 4. 仍需手测确认（UI行为）
- 阅读页首次进入：本地 PDF 不存在时是否自动下载后展示。
- 询问会话切换：历史消息与引用显示是否符合预期。
- 草稿恢复：刷新页面后阅读位置/批注/提问草稿恢复是否正确。
