# P0证据定位可用性与可读性修复 测试记录（2026-02-27 01:55）

## 测试环境
- 分支：`feature/reader-workbench-v2-phase12`
- 后端 Python 环境：`.venv-ragtest`
- 前端：Vite + TypeScript
- 容器：`docker compose`

## 执行命令与结果
1. 后端回归测试
```bash
$env:DEBUG='false'
.\.venv-ragtest\Scripts\python.exe -m pytest backend/tests/test_literature_reader_composed.py backend/tests/test_literature_reader_generative.py -q
```
结果：`38 passed`

2. 前端构建
```bash
npm run build
```
结果：通过（`tsc -b && vite build` 成功）

3. 容器同步
```bash
docker compose up -d --build backend frontend
docker compose ps
docker compose logs --tail 100 backend
docker compose logs --tail 100 frontend
```
结果：backend/frontend 已重建并正常启动。

## 关键验证点
- 长段落锚点可分片，证据卡可切换片段。
- `char_range` 不再固定截断前 14 行。
- 证据图裁剪失败时有 fallback，不再空白。
- FigurePanel 在无 `image_url` 时会尝试锚点截图，失败有重试占位。
- 浅色模式控件与文本对比度提升（按钮/标签/输入框/下拉）。
- `selected_kb_id` 仅在用户拥有时参与签名，防止跨租户探测。
- inline query 请求携带 `theme_mode` 与 `citation_tldr`，与 compose 视图一致。

## 已知事项
- 构建输出中仍有大包体 warning（非本次 P0 范围）。
- pytest 输出包含部分依赖层面的 deprecation warning（非本次改动引入）。
