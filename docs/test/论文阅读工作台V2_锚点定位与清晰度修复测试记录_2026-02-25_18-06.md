# 论文阅读工作台 V2：锚点定位与清晰度修复测试记录（2026-02-25 18:06）

## 测试环境
- 仓库：research-assistant
- 系统时间：2026-02-25

## 已执行
1. Python 语法检查：
- `python -m py_compile backend/app/services/literature_reader_compose_service.py backend/app/api/literature.py backend/app/schemas/literature.py`
- 结果：通过

2. 后端单测：
- 先安装缺失依赖：`python -m pip install pytest-asyncio`
- 执行：`pytest backend/tests/test_literature_reader_composed.py -q`
- 结果：`12 passed`

3. 前端构建：
- 执行：`npm run build`（frontend）
- 结果：通过

4. 容器回归：
- `docker compose up -d --build backend frontend`
- `docker compose ps`
- `docker compose logs --tail 80 backend`
- `docker compose logs --tail 80 frontend`
- 结果：backend/frontend 均正常启动

## 重点核验点
- composed stream 支持 `max_iterations` 参数透传。
- 证据预览卡位于批注区下方，不再悬浮遮挡正文。
- 证据卡显示原始 PDF 局部高亮截图与定位依据标签。
- 锚点定位优先走文本锚点（char range / quote），bbox 仅兜底。
