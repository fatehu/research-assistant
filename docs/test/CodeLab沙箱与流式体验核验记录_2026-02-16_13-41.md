# CodeLab 沙箱与流式体验核验记录（2026-02-16 13:41）

## 1. 环境
- 分支：`feature/codelab-rag-reasoning-notebook-fix-20260216`
- 服务：`backend`、`codelab-runner`、`frontend`（Docker）

## 2. 沙箱与服务核验
1. 容器状态
- 命令：`docker compose ps`
- 结果：`backend/codelab-runner/frontend/postgres/redis` 均为 `Up`。

2. Runner 健康检查 + 执行检查（容器内）
- 读取配置：
  - `CODELAB_RUNNER_URL=http://codelab-runner:8099`
  - `CODELAB_RUNNER_TOKEN` 已配置（仅校验长度，不记录明文）。
- 调用：
  - `GET /internal/healthz` 返回 `200`。
  - `POST /internal/codelab/execute`（Bearer token）返回 `200` 且 `success=True`。

3. 后端配置核验
- `codelab_sandbox_enabled=True`
- `codelab_runner_enabled=True`
- `codelab_direct_execute_enabled=False`

## 3. 自动化测试
1. 后端 codelab 相关快速回归
- 命令：
```powershell
docker compose exec -T backend pytest /app/tests/test_codelab_direct_execute_guard.py /app/tests/test_codelab_runner_unavailable.py /app/tests/test_codelab_policy_blocklist.py -q
```
- 结果：`6 passed`。

2. 前端静态检查与构建
- 命令：
```powershell
npm --prefix frontend run lint -- src/components/NotebookAgentPanel.tsx src/pages/chat/components/HistoryReActPanel.tsx
npm --prefix frontend run build
```
- 结果：构建通过；lint 仅存在项目既有 warnings，无新增 errors。

## 4. 结论
- CodeLab 沙箱链路正常（健康、鉴权、执行、后端配置均正常）。
- 推理面板已完成流式渲染优化并通过构建验证。
- 当前阻塞项：无。
