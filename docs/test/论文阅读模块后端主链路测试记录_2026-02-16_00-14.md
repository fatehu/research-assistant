# 论文阅读模块后端主链路测试记录（阶段）

- 时间：2026-02-16 00:14
- 环境：Windows + PowerShell，Python 虚拟环境 `.venv-ragtest`

## 1. 执行命令

1. 后端语法编译

```powershell
python -m py_compile backend/app/api/literature.py backend/app/schemas/literature.py backend/app/models/literature.py backend/alembic/versions/016_literature_reader_and_qa.py
```

2. 前端构建（TypeScript + Vite）

```powershell
npm --prefix frontend run build
```

3. 文献相关回归（定向）

```powershell
.\.venv-ragtest\Scripts\python -m pytest backend/tests/test_tool_architecture_refactor.py -k literature_service_multi_deduplicates_and_keeps_better_paper -q
```

4. 文献工具链回归（定向）

```powershell
Set-Location backend
$env:DATABASE_URL='postgresql://postgres:postgres@127.0.0.1:5432/postgres'
..\.venv-ragtest\Scripts\python -m pytest tests/test_tool_architecture_refactor.py -k literature_search_multi_and_default_source_compatible -q
```

5. Alembic 迁移校验

```powershell
Set-Location backend
..\.venv-ragtest\Scripts\python -m alembic upgrade head
```

6. 路由模块导入校验（FastAPI 依赖完整性）

```powershell
.\.venv-ragtest\Scripts\python -c "import sys; sys.path.insert(0, 'backend'); import app.api.literature as m; print(bool(m.router))"
```

## 2. 结果

- 后端 `py_compile`：通过
- 前端 `build`：通过（存在体积告警，无编译错误）
- 定向 pytest（2项）：通过
- Alembic `upgrade head`：失败
- 文献路由模块导入：通过

## 3. Alembic 失败原因与处理

- 初次失败：`alembic.ini` 在 GBK locale 下解码异常
- 已处理：将 `backend/alembic.ini` 注释调整为 ASCII，消除编码阻塞
- 当前剩余失败：数据库连接认证失败（`InvalidPasswordError`），属于环境凭据问题，不属于代码逻辑问题

## 4. 结论

- 本阶段代码变更在“语法 + 前端类型 + 定向回归”层面可用。
- 迁移执行仍需提供可用数据库账号/密码后再完成最终验证。
