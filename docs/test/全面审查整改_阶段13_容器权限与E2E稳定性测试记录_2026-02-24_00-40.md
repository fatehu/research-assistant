# 全面审查整改_阶段13_容器权限与E2E稳定性测试记录

- 记录时间：2026-02-24 00:40
- 环境：Windows + Docker Desktop + 分支 `feature/full-remediation-2026-02`
- 后端地址：`http://localhost:8888`
- 前端地址：`http://localhost:3000`

## 1. 后端门禁与关键回归
1. `python backend/checks/check_no_new_broad_excepts.py` -> 通过。
2. `python backend/checks/check_contract_alignment.py` -> 通过。
3. 关键集：
- `test_codelab_runner_unavailable.py` -> 通过。
- `test_codelab_timeout_enforced.py` -> 通过。
- `test_agent_runtime_service.py` -> 通过（sqlite 测试 URL）。
- `test_literature_reader_api.py` -> 通过（sqlite 测试 URL）。

## 2. 后端全集测试（证据）
1. `python -m pytest backend/tests -q` -> 失败（6 failed, 28 errors, 242 passed）。
2. 失败主要集中在既有历史模块：
- `smart_chunking_service` 相关类导入与 API 兼容性。
- `codelab.notebook_agent_chat` 历史测试期望与当前实现不一致。
3. 判定：本轮改动未新增该类失败，属于基线遗留问题，已保留证据供后续专项治理。

## 3. 前端质量
1. `npm run lint` -> 通过（14 条 `react-hooks/exhaustive-deps` warning）。
2. `npm run build` -> 通过（大 chunk 告警仍存在，构建不阻断）。

## 4. 容器闭环
1. `docker compose up -d --build backend frontend` -> 通过。
2. `docker compose ps` -> `backend/frontend/postgres/redis/codelab-runner` 均 `Up`。
3. 日志核验：
- 后端启动、迁移、请求链路正常。
- 前端 dev server 正常监听 3000。

## 5. E2E / 验收脚本
1. `./acceptance_tests.ps1` -> 通过。
2. `./e2e/role_business_api_smoke_e2e.ps1` -> 通过。
3. `./e2e/remaining_modules_api_smoke_e2e.ps1` -> 初次失败，修复后通过。
4. `./e2e/student_role_smoke_e2e.ps1` -> 通过。
5. `./e2e/role_route_smoke_e2e.ps1 -Role mentor` -> 修复后通过。
6. `./e2e/role_route_smoke_e2e.ps1 -Role admin` -> 修复后通过。
7. `./e2e/mcp_settings_login_e2e.ps1` -> 初次失败，修复后通过。
8. `./e2e/knowledge_upload_pipeline_e2e.ps1` -> 通过。

## 6. 失败分类与修复结论
1. 代码缺陷（已修复）
- 非 root 后上传目录不可写（`/app/uploads/*`）。
- 非 root 后 MCP 配置文件不可写（`mcp_servers.json`）。

2. 脚本缺陷（已修复）
- `role_route_smoke_e2e.ps1` 对 `open <url>` 依赖过强，导致浏览器初始化阶段高频波动。

3. 环境缺陷（已记录）
- 宿主 Python3.13 依赖生态兼容性（`asyncpg` 编译/安装链路）仍需长期方案。
