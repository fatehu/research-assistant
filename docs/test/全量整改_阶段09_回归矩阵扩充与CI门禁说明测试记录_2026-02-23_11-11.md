# 全量整改 阶段09 测试记录：回归矩阵扩充与 CI 门禁说明

- 时间：2026-02-23 11:11
- 测试人：Codex
- 分支：`feature/full-remediation-2026-02`

## 环境

- 操作系统：Windows（PowerShell）
- 编码前置：`chcp 65001`
- Python：系统 Python 3.13（未安装完整后端依赖）
- Node：`npm ci` 已执行

## 命令与结果

1. `python backend/checks/check_no_new_broad_excepts.py`
- 结果：通过

2. `python backend/checks/check_contract_alignment.py`
- 结果：通过

3. `python -m pytest backend/tests/test_contract_alignment_check.py -q`
- 结果：通过（`3 passed`）

4. `npm ci`（`frontend/`）
- 结果：通过

5. `npm run lint`（`frontend/`）
- 结果：通过（0 error，14 warning，均为历史 `react-hooks/exhaustive-deps` 警告）

6. `npm run build`（`frontend/`）
- 结果：通过（Vite 构建完成）

7. `python -m pytest backend/tests/test_codelab_runner_unavailable.py -q`
- 结果：失败（缺依赖 `loguru`）

8. `python -m pytest backend/tests/test_codelab_timeout_enforced.py -q`
- 结果：失败（缺依赖 `loguru`）

9. `python -m pytest backend/tests/test_agent_runtime_service.py -q`
- 结果：失败（缺依赖 `loguru`）

10. `python -m pytest backend/tests/test_literature_reader_api.py -q`
- 结果：失败（缺依赖 `fastapi`）

11. `./acceptance_tests.ps1`
- 结果：失败（`Unable to connect to the remote server`）

12. `./e2e/role_business_api_smoke_e2e.ps1`
- 结果：失败（后端不可达，`http://localhost:8888` 连接失败）

13. `./e2e/remaining_modules_api_smoke_e2e.ps1`
- 结果：失败（后端不可达，`http://localhost:8888` 连接失败）

## 失败定位与处置

- 后端 pytest 失败为环境依赖缺失（`loguru`、`fastapi`），非本次改动引入。
- E2E 脚本失败为本地后端服务未启动导致连接失败。
- 已将可在当前环境验证的门禁与前端构建链路全部跑通。

## 回放清单

1. 补齐后端依赖（建议使用项目虚拟环境，如 `.venv-ragtest`）。
2. 启动容器或本地后端服务后重跑 7-13 项。
3. CI 环境以 workflow 为准持续验证新增门禁。

## 结论

- 阶段9新增门禁能力与 CI 接入有效。
- 受环境限制，后端关键回归与 E2E 未在本地完成闭环，已记录阻塞与回放步骤。
