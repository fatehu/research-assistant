# AgentCore统一化阶段3：论文阅读工作台与询问链路增强测试记录（2026-02-16 08:14）

## 1. 测试环境
- 分支：`feature/agentcore-unify-memory-20260216`
- 平台：Windows PowerShell

## 2. 测试命令
1. 后端语法编译：
```bash
python -m py_compile backend/app/api/literature.py backend/app/schemas/literature.py backend/app/main.py
```
2. 前端构建：
```bash
npm --prefix frontend run build
```

## 3. 测试结果
- `py_compile`：通过。
- 前端 `build`：通过（存在历史 chunk-size warning，无新增构建错误）。

## 4. 手工验证要点
- 阅读页可加载 PDF，并在文本模式下显示结构化内容。
- 问答来源支持编号、页码/章节信息展示与跳转。
- 自动保存状态可见，收藏夹范围可提示“未入库论文不会参与联合回答”。

## 5. 结论
- 阶段3功能可进入提交。