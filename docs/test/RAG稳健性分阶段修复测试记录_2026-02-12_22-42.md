# RAG稳健性分阶段修复测试记录（2026-02-12 22:42）

## 1. 测试目的
验证分阶段修复后的关键能力：
1. Notebook Agent 异步 LLM 初始化正确。  
2. ReAct 引用校验与引用修复生效。  
3. 压缩降级路径仍保留 `[来源X]`。  
4. Agent 检索阈值改为配置驱动且不再硬编码。  
5. 既有检索增强功能无回归。

## 2. 环境
1. 本地测试解释器：`.venv`  
2. 系统主运行环境：Docker（本次为代码级回归测试，不依赖完整端到端服务）。

## 3. 执行命令

```powershell
.venv\Scripts\python -m pytest backend/tests/test_query_rewrite_service.py backend/tests/test_hybrid_retrieval_service.py backend/tests/test_contextual_compression_service.py backend/tests/test_vector_search_tuning.py backend/tests/test_react_agent_citation_policy.py backend/tests/test_tool_registry_knowledge_search.py backend/tests/test_agent_db_wiring.py -q
```

## 4. 结果
输出摘要：

```text
...........................                                              [100%]
27 passed in 1.37s
```

结论：本次修复范围内测试全部通过。

## 5. 新增/强化测试点
1. `backend/tests/test_agent_db_wiring.py`
- 验证 notebook agent 聊天路由使用 `await get_llm_service()`。  
- 验证 agent 路由具备 `db` 依赖接线。

2. `backend/tests/test_react_agent_citation_policy.py`
- 验证来源标签提取与引用合法性判断。  
- 验证缺失引用时可触发修复。  
- 验证压缩降级场景仍包含 `[来源X]`。

3. `backend/tests/test_tool_registry_knowledge_search.py`
- 验证 KnowledgeSearchTool 使用 `settings.agent_knowledge_score_threshold`。  
- 验证 SQL 使用参数化阈值 `:distance_threshold`，避免硬编码常量。

## 6. 风险与后续建议
1. 当前以单元测试为主，建议补充端到端引用一致性测试（真实对话输入 -> 输出来源校验）。  
2. 建议增加性能基线测试（P95 延迟、不同阈值下召回/准确率对比）。

