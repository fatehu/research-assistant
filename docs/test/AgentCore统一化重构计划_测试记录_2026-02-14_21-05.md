# AgentCore统一化重构计划 测试记录

## 1. 测试环境
- 系统：Windows（PowerShell）
- Python：`.venv-ragtest`（Python 3.13）
- 测试命令执行器：`python -m pytest`

## 2. 依赖补齐（本地）
为保证 API/Schema 相关测试可收集，补齐了以下缺失依赖：
- `fastapi==0.115.0`
- `sse-starlette==2.1.0`
- `python-jose==3.3.0`
- `passlib==1.7.4`
- `bcrypt==4.1.2`
- `email-validator==2.2.0`

说明：尝试整包安装 `backend/requirements.txt` 时，`pandas==2.2.0` 在 Python 3.13 环境下需要本地编译并失败（`stdalign.h` 缺失），因此采用最小依赖补齐策略完成本次回归。

## 3. 执行命令
```powershell
.\.venv-ragtest\Scripts\python -m pytest -q `
  backend/tests/test_react_agent_citation_policy.py `
  backend/tests/test_contextual_compression_batch_pipeline.py `
  backend/tests/test_agent_db_wiring.py `
  backend/tests/test_tool_architecture_refactor.py `
  backend/tests/test_tool_registry_mcp_bridge.py `
  backend/tests/test_tool_registry_mcp_routing.py `
  backend/tests/test_agent_core_unified_routes.py `
  backend/tests/test_agent_context_budget.py `
  backend/tests/test_agent_function_calling_fallback.py `
  backend/tests/test_agent_parallel_calls.py `
  backend/tests/test_agent_step_persistence.py
```

## 4. 结果
- 结果：`55 passed, 4 warnings`
- 关键新增用例结果：
  - Agent 双入口统一：通过
  - 上下文预算裁剪：通过
  - function calling -> XML fallback：通过
  - 并行工具调用：通过
  - run/step 持久化：通过

## 5. 问题与修复
- 问题：`SYSTEM_PROMPT` 的 JSON 示例未转义，触发 `str.format KeyError('"tool"')`。
- 修复：将 JSON 花括号改为双花括号转义，恢复提示词构建与全链路执行。

## 6. 结论
- 本次 Agent Core 统一改造在既有回归范围内通过，新增能力具备可测闭环。
- 后续进入容器冒烟前，建议先固定一套 Python 版本（优先 3.11/3.12）避免重依赖在 3.13 的编译不确定性。

