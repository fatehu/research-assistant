# 全面巡检执行记录
- 时间：2026-02-24 13:06
- 执行分支：`main`
- 执行环境：Windows + Docker Compose（`backend:8888`, `frontend:3000`）

## 1. 基线与门禁
1. `python backend/checks/check_no_new_broad_excepts.py`  
结果：PASS
2. `python backend/checks/check_contract_alignment.py`  
结果：PASS
3. `python -m pytest backend/tests/test_contract_alignment_check.py -q`  
结果：PASS（5 passed）
4. `npm run lint`（frontend）  
结果：PASS（0 error，14 warning）
5. `npm run build`（frontend）  
结果：PASS（仅 chunk size warning）

## 2. 自动化回归矩阵
1. `./acceptance_tests.ps1`  
结果：PASS（ALL TESTS PASSED）
2. `./e2e/role_business_api_smoke_e2e.ps1`  
结果：PASS
3. `./e2e/remaining_modules_api_smoke_e2e.ps1`  
结果：PASS
4. `./e2e/student_role_smoke_e2e.ps1`  
结果：PASS
5. `./e2e/role_route_smoke_e2e.ps1 -Role mentor`  
结果：PASS
6. `./e2e/role_route_smoke_e2e.ps1 -Role admin`  
结果：PASS
7. `./e2e/mcp_settings_login_e2e.ps1`  
结果：PASS
8. `./e2e/knowledge_upload_pipeline_e2e.ps1`  
结果：PASS

## 3. 关键补充探针
1. 聊天流式探针：`/api/v1/chat/send`  
结果：收到 `start/content/done`，无 `error`
2. 限流探针：高频请求 `/api/v1/auth/login`  
结果：第 20 次触发 429，`X-RateLimit-Limit=20`，`X-RateLimit-Remaining=0`
3. Smart Chunking 补测：`/chunking/compare` + `apply-preset` + `config`  
结果：PASS，持久化策略回读一致
4. 文献深链路探针：PDF/reader session/annotation/comment/rating/add-to-knowledge/ask  
结果：PASS（`knowledge_status=completed`，`ask` 返回 `done` + `sources`）
5. CodeLab Agent 探针：chat/history/suggest-code/explain-error  
结果：PASS（`chat_done=true`，`history 2->0`）

## 4. 容器与日志
1. `docker compose ps`  
结果：`backend/frontend/codelab-runner/postgres/redis` 全部 Up
2. `docker compose logs --tail 200 backend`  
结果：未见持续阻断错误
3. `docker compose logs --tail 200 frontend`  
结果：未见持续阻断错误

## 5. 过程异常与处理
1. `role_route_smoke` 并行执行时出现 Playwright 会话名冲突。  
处理：改串行执行 mentor/admin，均 PASS。
2. PowerShell + curl 直接传 JSON 时出现 `json_invalid`。  
处理：改为写入 UTF-8 JSON 文件后 `--data-binary @file`，恢复正常。
3. 宿主机本地 `pytest` 缺少 `loguru`。  
处理：关键后端测试改在容器内执行，测试通过。

## 6. 结论
1. P0：0  
2. P1：0  
3. 可合并：是  
4. 可发布：是（存在 P3 优化项：前端 hooks warning、console error 基线清理）
