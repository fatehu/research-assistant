# 论文阅读器 AI 组件编排视图与乱码修复测试记录（2026-02-25 02:05）

## 1. 测试环境
- OS: Windows (PowerShell)
- Python: `.venv-ragtest`
- Frontend: Vite + TypeScript
- 容器：Docker Compose

## 2. 执行命令与结果

### 2.1 后端语法检查
- 命令：
  - `python -m py_compile backend/app/api/literature.py backend/app/schemas/literature.py backend/app/services/literature_reader_compose_service.py backend/app/services/literature_reader_service.py`
- 结果：通过

### 2.2 前端构建
- 命令：
  - `cd frontend && npm run build`
- 结果：通过（Vite 打包完成，仅有 chunk size warning）

### 2.3 既有生成式阅读回归
- 命令：
  - `& .\.venv-ragtest\Scripts\python -m pytest backend/tests/test_literature_reader_generative.py -q`
- 结果：`12 passed`

### 2.4 新增 composed 单测
- 文件：`backend/tests/test_literature_reader_composed.py`
- 命令：
  - `& .\.venv-ragtest\Scripts\python -m pytest backend/tests/test_literature_reader_composed.py -q`
- 结果：`7 passed`
- 覆盖点：
  - 质量阈值达标停机
  - 预算超时 best-effort 返回
  - 外网图归因字段强约束
  - PDF 图优先时不触发外网搜索
- 预读去重/边界裁剪
- 非法 anchor 校验
- SSE 事件顺序校验（composed stream）

### 2.5 容器回归
- 命令：
  - `docker compose up -d --build backend frontend`
  - `docker compose ps`
  - `docker compose logs --tail 100 backend`
  - `docker compose logs --tail 100 frontend`
- 结果：
  - backend/frontend 成功 build 并启动
  - 后端启动日志正常（含 Alembic 上下文与 Uvicorn 启动）
  - 前端 Vite dev 服务正常监听 `http://localhost:3000`

## 3. 失败与修复记录
- 问题：`backend/app/api/literature.py` 曾出现字符串破损导致语法错误。
- 修复：逐段修复破损引号/括号并完成 `py_compile` 验证。
- 问题：开发过程中出现“终端显示乱码”。
- 修复：统一使用 `chcp 65001` + `Get-Content/Set-Content -Encoding utf8`。

## 4. 手测建议（待你确认）
- 打开 `http://localhost:3000/literature/reader/<paper_id>`：
  - 文本模式应优先走 AI 组件编排视图。
  - 点击“重新生成”应触发新一轮 composed stream。
  - 切页后应触发邻页预读（标签可见“已预读”）。
  - 后端异常时应回落到本地结构化文本，而不是空白页。

## 5. 结论
- 本轮代码已满足“可构建、可运行、可回归、可降级”。
- 主链路（composed stream + 组件渲染 + 预读）已落地。
- 文档与测试记录已按 `code.md` 补齐。
