# 检索性能优化阶段5测试记录

- 记录时间：2026-02-13 16:27
- 测试范围：上下文摘要入库、相邻窗口检索、回填/重建脚本可执行性、阶段回归稳定性。

## 环境
- 本地测试环境：`.venv-ragtest`（Python 3.13.3）
- 容器环境：`research_backend` + `research_postgres`

## 一、单元/集成测试
### 1) Phase5 关键测试
```bash
.\\.venv-ragtest\\Scripts\\python.exe -m pytest \
  backend/tests/test_contextual_retrieval_window.py \
  backend/tests/test_tool_registry_knowledge_search.py \
  backend/tests/test_vector_search_tuning.py -q
```
结果：`13 passed in 1.73s`

### 2) 检索优化回归集（Phase1~5）
```bash
.\\.venv-ragtest\\Scripts\\python.exe -m pytest \
  backend/tests/test_contextual_compression_service.py \
  backend/tests/test_contextual_compression_batch_pipeline.py \
  backend/tests/test_chinese_fts_segmented.py \
  backend/tests/test_query_rewrite_service.py \
  backend/tests/test_vector_search_tuning.py \
  backend/tests/test_tool_registry_knowledge_search.py \
  backend/tests/test_contextual_retrieval_window.py -q
```
结果：`35 passed in 3.52s`

## 二、迁移与脚本验证
### 1) 迁移升级
```bash
docker exec research_backend alembic upgrade head
```
结果：成功升级到 `011_context_summary`。

### 2) backfill dry-run
```bash
docker exec research_backend python -m app.maintenance.backfill_context_summary_and_embeddings \
  --mode backfill --batch-size 50 --limit 5 --dry-run
```
结果：执行成功，日志输出 `processed/updated`。

### 3) rebuild dry-run
```bash
docker exec research_backend python -m app.maintenance.backfill_context_summary_and_embeddings \
  --mode rebuild --truncate-first --batch-size 2 --limit 2 --dry-run
```
结果：执行成功，日志输出 `processed/failed/recount`。

### 4) rebuild 小批量实跑（非 dry-run）
```bash
docker exec research_backend python -m app.maintenance.backfill_context_summary_and_embeddings \
  --mode rebuild --batch-size 1 --limit 1
```
结果：执行成功，文档状态判定为 `COMPLETED`，并完成 KB 统计回算。

## 三、问题与修复记录
- 发现：`010_add_content_segmented_and_fts_index` 的 revision id 超过 Alembic 默认 `varchar(32)`，导致 `alembic_version` 更新失败。
- 修复：将 revision id 缩短为 `010_content_segmented_fts`、`011_context_summary`，并将迁移 DDL 改为幂等写法。
- 验证：`alembic upgrade head` 可成功执行。

## 结论
- Phase5 目标能力已可用：`context_summary` + `adjacent_context` + 可执行回填/重建脚本。
- 阶段回归稳定，未引入 Phase1~4 回归失败。
