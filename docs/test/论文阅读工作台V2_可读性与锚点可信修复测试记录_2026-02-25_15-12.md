# 论文阅读工作台 V2 可读性与锚点可信修复测试记录

## 测试时间
- 2026-02-25 15:12

## 测试环境
- 本地：Windows + PowerShell
- Python：`.venv-ragtest`
- 前端构建：Vite + TypeScript
- 容器：Docker Compose

## 执行命令与结果

1. Python 语法检查
- 命令：`python -m py_compile backend/app/services/literature_reader_compose_service.py`
- 结果：通过

2. 后端单测（compose）
- 命令：`.\.venv-ragtest\Scripts\python.exe -m pytest backend/tests/test_literature_reader_composed.py -q`
- 结果：`12 passed`
- 备注：存在 pytest_asyncio/event_loop 兼容性警告，不影响本次功能正确性。

3. 前端构建
- 命令：`npm run build`（目录：`frontend`）
- 结果：通过

4. 容器回归
- 命令：`docker compose up -d --build backend frontend`
- 结果：成功启动
- 健康检查：`docker compose ps` 状态正常（backend/frontend/postgres/redis 均 Up）

5. 容器日志检查
- 命令：
  - `docker compose logs --tail 60 backend`
  - `docker compose logs --tail 60 frontend`
- 结果：无启动错误，服务可用。

## 新增回归用例
- `test_reader_compose_sanitize_anchor_can_recover_invalid_range`
- `test_reader_compose_normalize_blocks_merge_split_heading_lines`
- `test_inline_query_slot_should_resolve_target_node_text`

## 结论
- 浅色模式可读性问题已通过作用域样式隔离修复。
- 内联问答已从“占位节点上下文”升级为“目标节点+邻近证据上下文”。
- 锚点合法性与标题拆分问题已在后端主链路修复，并由单测覆盖。
