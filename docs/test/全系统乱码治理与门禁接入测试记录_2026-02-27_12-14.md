# 全系统乱码治理与门禁接入测试记录

## 测试时间
- 2026-02-27 12:14（本地）

## 环境
- OS: Windows（PowerShell）
- Python: 本地 Python 3.x
- 测试框架：pytest

## 测试项与结果
1. 乱码检测脚本（严格模式）  
   - 命令：`python scripts/check_mojibake.py --strict`  
   - 预期：返回 0（扫描范围内无乱码）  
   - 结果：通过  
2. 乱码检测脚本（JSON 输出）  
   - 命令：`python scripts/check_mojibake.py --strict --json`  
   - 预期：输出 `issue_count` 与 `issues` 结构  
   - 结果：通过  
3. 检测脚本单元测试  
   - 命令：`python -m pytest tests/test_check_mojibake.py -q`  
   - 预期：4 个测试全部通过  
   - 结果：通过  

## 回归检查
1. `scripts/code.md` 新增标准巡检命令。  
2. CI 工作流已接入 `Mojibake guard` 步骤。  
3. 文档示例命令已替换为统一脚本命令。  

## 结论
1. 乱码治理能力已形成“本地可执行 + CI 可阻断”的闭环。  
2. 本轮范围（主代码+文档，排除日志产物）达成。  

