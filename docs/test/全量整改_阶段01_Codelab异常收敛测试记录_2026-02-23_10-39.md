# 全量整改阶段01：CodeLab 宽异常收敛测试记录

- 时间：2026-02-23 10:39
- 测试环境：Windows PowerShell，本地 Python 3.13，Docker Desktop 未启动。
- 关联 commit：`fix(codelab): 收敛宽异常并修复门禁超线`

## 执行命令与结果
1. `python backend/checks/check_no_new_broad_excepts.py`
   - 结果：通过（`Broad exception guard passed.`）。
2. `docker compose up -d --build backend codelab-runner postgres redis`
   - 结果：失败。
   - 原因：Docker 引擎不可用，报错 `open //./pipe/dockerDesktopLinuxEngine: The system cannot find the file specified.`
3. `docker compose exec -T backend python -m pytest backend/tests/test_codelab_runner_unavailable.py -q`
   - 结果：未执行（依赖容器不可用）。
4. `docker compose exec -T backend python -m pytest backend/tests/test_codelab_timeout_enforced.py -q`
   - 结果：未执行（依赖容器不可用）。

## 失败与修复
1. 阻塞点：本机 Docker Desktop 未启动，导致容器测试链路不可达。
2. 代码层面：已完成门禁修复，无新增失败。

## 结论
1. 阶段1核心目标（宽异常门禁恢复）已达成。
2. 待 Docker 恢复后补跑容器测试并追加记录。
