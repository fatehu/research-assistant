# 论文阅读工作台 V2 阶段一+阶段二测试记录

## 1. 测试环境
- 时间：2026-02-25 03:56
- 系统：Windows + PowerShell
- 前端：Vite + React + TypeScript
- 后端：FastAPI + pytest

## 2. 执行命令
1. 后端 composed 回归
```bash
.\.venv-ragtest\Scripts\python.exe -m pytest backend/tests/test_literature_reader_composed.py -q
```

2. 前端构建
```bash
cd frontend
npm run build
```

## 3. 结果
### 3.1 后端测试
- 结果：`7 passed`
- 结论：
  - composed SSE 事件序列正确。
  - quality threshold / budget timeout 逻辑正确。
  - external image attribution 约束正常。
  - prefetch 去重与边界逻辑正常。

### 3.2 前端构建
- 结果：`tsc -b && vite build` 成功。
- 结论：
  - 新增的节点动作、内联问答、PiP、拖拽、主题/颗粒度控制均通过编译。

## 4. 本轮重点验收项对照
1. 节点修复/降级：已接通 node action API，支持局部替换。
2. 内联问答：已接通 SSE，支持原位插入 AnswerCard。
3. 证据预览：已支持 hover 预览 + click 钉住 + 关闭。
4. 拖拽沉淀：已支持组件 Markdown 拖入批注输入区。
5. 模式记忆：已持久化主题/颗粒度/compare/citation 等阅读参数。
6. 乱码修复：阅读主链路相关提示词与错误文案已修复。

## 5. 已知问题
1. `backend/app/api/literature.py` 仍有少量历史乱码注释（非核心用户路径），建议后续全量清理。
2. PiP 当前是文本证据预览，尚未升级为 PDF 局部截图高亮版本。

## 6. 结论
- 阶段一核心闭环已可用：可信验证 + 局部操作 + 内联问答 + 拖拽沉淀。
- 阶段二入口参数已贯通：detail/compare/citation/theme 均可驱动 composed 构建链路。
