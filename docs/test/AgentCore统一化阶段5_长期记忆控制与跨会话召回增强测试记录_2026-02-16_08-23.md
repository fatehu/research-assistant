# AgentCore统一化阶段5：长期记忆控制与跨会话召回增强测试记录（2026-02-16 08:23）

## 1. 测试环境
- 分支：`feature/agentcore-unify-memory-20260216`
- Python：`.venv-ragtest`

## 2. 测试命令
1. 后端语法编译：
```bash
.venv-ragtest/Scripts/python -m py_compile \
  backend/app/api/codelab.py \
  backend/app/services/agent_runtime_service.py \
  backend/app/services/react_agent.py \
  backend/app/config.py
```
2. 路由挂载检查：
```bash
PYTHONPATH=backend .venv-ragtest/Scripts/python - << 'PY'
from app.main import app
need = {
  ('GET','/api/v1/codelab/agent/memory/settings'),
  ('PUT','/api/v1/codelab/agent/memory/settings'),
  ('DELETE','/api/v1/codelab/agent/memory'),
}
have = set()
for r in app.routes:
    for m in (r.methods or set()):
        if m in {'HEAD', 'OPTIONS'}:
            continue
        have.add((m, r.path))
assert not (need - have)
print('MEMORY_ROUTES_OK')
PY
```

## 3. 测试结果
- `py_compile`：通过。
- memory 接口路由检查：通过（`MEMORY_ROUTES_OK`）。

## 4. 结论
- 阶段5可进入提交。