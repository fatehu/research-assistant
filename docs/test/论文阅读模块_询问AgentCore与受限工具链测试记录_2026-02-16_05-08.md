# 论文阅读模块：询问 Agent Core 与受限工具链测试记录（2026-02-16 05:08）

## 1. 测试环境
- OS: Windows（PowerShell）
- 后端：FastAPI + SQLAlchemy（源码编译检查）
- 前端：TypeScript + Vite（生产构建检查）

## 2. 测试命令
1. `python -m compileall backend/app/api/literature.py backend/app/schemas/literature.py`
2. `cd frontend && npm run build`

## 3. 执行结果
### 3.1 后端编译
- 结果：通过
- 关键输出：
  - `Compiling 'backend/app/api/literature.py'...`
  - `Compiling 'backend/app/schemas/literature.py'...`

### 3.2 前端构建
- 结果：通过
- 关键输出：
  - `tsc -b && vite build`
  - `✓ built in 12.90s`

## 4. 本阶段验证点（代码级）
1. `mode` 字段联通：
- 后端请求模型支持 `agentic|classic`。
- 前端请求类型与 UI 模式选择已同步。

2. Agent 工具白名单：
- `knowledge_search` 替换为论文范围受限实现。
- 本地工具与 MCP 工具均按白名单过滤。

3. 引用格式：
- Agent 输出 `[来源X]` 时，前端可解析并关联来源跳转。

## 5. 已知限制
- 本记录主要覆盖静态编译/构建与实现联通，未包含端到端实时提问实测截图。
- MCP 实际调用成功率受部署环境配置（路由/API Key/网络）影响。

## 6. 结论
- 本阶段代码可编译、可构建，Agent Core 询问链路与工具白名单机制已落地，并保留 classic 兜底路径。

