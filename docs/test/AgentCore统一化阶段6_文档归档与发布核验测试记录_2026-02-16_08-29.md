# AgentCore统一化阶段6：文档归档与发布核验测试记录（2026-02-16 08:29）

## 1. 测试环境
- 分支：`feature/agentcore-unify-memory-20260216`
- Python：`.venv-ragtest`
- Docker：`research-assistant` compose 项目

## 2. 测试命令
1. 快速语法回归：
```bash
.venv-ragtest/Scripts/python -m py_compile \
  backend/app/main.py \
  backend/app/api/codelab.py \
  backend/app/api/literature.py \
  backend/app/api/knowledge.py \
  backend/app/services/react_agent.py \
  backend/app/services/agent_runtime_service.py
```
2. 容器重建发布：
```bash
CODELAB_RUNNER_TOKEN=local_dummy_token docker compose up -d --build backend frontend
```
3. 健康核验：
```bash
docker compose ps
docker compose logs --tail 100 backend
docker compose logs --tail 100 frontend
```

## 3. 测试结果
- `py_compile`：通过。
- Docker 重建：通过（backend/frontend/codelab-runner 镜像重建并容器启动）。
- `ps/logs`：服务在线，未发现阻断级报错。

## 4. 结论
- 阶段6可进入提交，版本可用于手测与 PR 合并。