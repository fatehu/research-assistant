# 全面审查动态测试证据（2026-02-23 20:13）

## 1. 执行环境与基线
- 执行时间：2026-02-23 19:58 ~ 20:12（UTC+8，分钟级记录）
- 分支：`feature/full-remediation-2026-02`
- 代码工作区状态：`git status --short` 为空
- 容器入口：
  - Backend：`http://localhost:8888`
  - Frontend：`http://localhost:3000`

## 2. 命令矩阵与结果
| 序号 | 命令 | 结果 | 备注 |
|---|---|---|---|
| 1 | `python backend/checks/check_no_new_broad_excepts.py` | PASS | `Broad exception guard passed.` |
| 2 | `python backend/checks/check_contract_alignment.py` | PASS | `Contract alignment guard passed.` |
| 3 | `python -m pytest backend/tests/test_contract_alignment_check.py -q` | PASS | `3 passed in 0.03s` |
| 4 | `python -m pytest backend/tests -q` | FAIL | collection 阶段 39 errors（缺依赖） |
| 5 | `npm run lint` | PASS(WARN) | 14 条 `react-hooks/exhaustive-deps` warning |
| 6 | `npm run build` | PASS(WARN) | 分包失效 warning + 大 chunk warning |
| 7 | `./acceptance_tests.ps1` | PASS | 全部 A-F 测试通过 |
| 8 | `./e2e/role_business_api_smoke_e2e.ps1` | PASS | 角色业务 API 全通过 |
| 9 | `./e2e/remaining_modules_api_smoke_e2e.ps1` | PASS | 本轮未触发 429 |
| 10 | `./e2e/student_role_smoke_e2e.ps1` | PASS | UI 路由与权限通过 |
| 11 | `./e2e/role_route_smoke_e2e.ps1 -Role mentor`（并发首轮） | FAIL | 429 限流 |
| 12 | `./e2e/role_route_smoke_e2e.ps1 -Role admin`（并发首轮） | FAIL | 429 限流 |
| 13 | `Start-Sleep 40; ./e2e/role_route_smoke_e2e.ps1 -Role mentor` | PASS | 串行重跑通过 |
| 14 | `Start-Sleep 40; ./e2e/role_route_smoke_e2e.ps1 -Role admin` | PASS | 串行重跑通过 |
| 15 | `./e2e/mcp_settings_login_e2e.ps1` | PASS | MCP 设置页流程通过 |
| 16 | `./e2e/knowledge_upload_pipeline_e2e.ps1` | PASS | 上传→处理→检索通过 |
| 17 | `docker compose ps` | PASS | backend/frontend/postgres/redis/codelab-runner 均 up |
| 18 | `docker compose logs --tail 200 backend` | PASS(WARN) | MCP discover failed 警告可见 |
| 19 | `docker compose logs --tail 200 frontend` | PASS | 前端服务正常启动 |

## 3. 失败与告警证据
### 3.1 后端全集 pytest 失败（环境缺陷）
- 触发命令：`python -m pytest backend/tests -q`
- 现象：
  - `ModuleNotFoundError: No module named 'fastapi'`
  - `ModuleNotFoundError: No module named 'loguru'`
  - `ModuleNotFoundError: No module named 'sqlalchemy'`
  - `ModuleNotFoundError: No module named 'pydantic_settings'`
  - `PytestUnknownMarkWarning: Unknown pytest.mark.asyncio`
- 结论：本次失败属于“环境缺陷”，不是单一业务逻辑回归。

### 3.2 E2E 并发触发限流（测试脚本缺陷 + 环境参数敏感）
- 触发命令：
  - `./e2e/role_route_smoke_e2e.ps1 -Role mentor`
  - `./e2e/role_route_smoke_e2e.ps1 -Role admin`
- 首轮错误：
  - `{"detail":{"code":"rate_limited","message":"请求过于频繁，请稍后重试","retry_after_seconds":36}}`
- 二次验证：
  - 等待 40 秒后串行执行两条命令均 PASS。
- 结论：链路可用，失败主因是速率限制与脚本无退避重试策略。

### 3.3 前端构建告警
- 触发命令：`npm run build`
- 关键告警：
  - `frontend/src/services/api.ts` 同时被动态与静态引入，动态拆包失效。
  - 存在大体积 chunk（`index-CXPSGrLO.js`、`vendor-antd-Dsaz6fa9.js`）。

### 3.4 前端运行期告警
- 证据文件：`.playwright-cli/console-2026-02-23T12-08-15-976Z.log`
- 关键行：
  - 第 3 行：`[antd: message] Static function can not consume context`
  - 第 16 行：`[antd: Select] dropdownStyle is deprecated`
  - 第 1/2 行及后续多行：React Router v7 future flag warning

### 3.5 容器运行观测告警
- 触发命令：`docker compose logs backend | Select-String "discover failed|MCP"`
- 关键日志：
  - `app.services.mcp.client:discover_tools - [MCP] discover failed on server=filesystem: [Errno 2] No such file or directory`
- 同时观测：`/api/v1/mcp/status/refresh` 返回 200。

## 4. 失败分类结论（按蓝图要求）
- 代码缺陷：
  - 本轮动态测试未出现“可稳定复现的核心业务失败”。
- 环境缺陷：
  - 宿主 Python 环境缺少后端依赖导致 `pytest backend/tests -q` collection 失败。
- 测试脚本缺陷：
  - `e2e/role_route_smoke_e2e.ps1`、`e2e/remaining_modules_api_smoke_e2e.ps1` 请求封装无 429 退避重试。

## 5. 动态审查结论
- 指定 E2E 流程已全部打通（首轮并发限流后，经串行重跑验证链路可用）。
- 动态层主要风险来自“测试稳定性与环境一致性”，而非当前链路直接不可用。
- 对应修复动作已在《全面审查_修复蓝图_2026-02-23_20-13.md》给出。

