# 全项目修复计划（安全优先，双阶段）测试记录

## 1. 环境
- OS: Windows
- Python: `.venv-ragtest\Scripts\python`
- Node: npm（frontend）

## 2. 后端新增测试执行

### 2.1 执行命令
```powershell
.\\.venv-ragtest\\Scripts\\python -m pytest -q `
  backend/tests/test_codelab_timeout_enforced.py `
  backend/tests/test_codelab_policy_blocklist.py `
  backend/tests/test_codelab_direct_execute_guard.py `
  backend/tests/test_cors_origin_whitelist.py `
  backend/tests/test_auto_create_tables_guard.py `
  backend/tests/test_mentor_activities_api.py
```

### 2.2 结果
- 结果：`10 passed`
- 结论：
  - `while True` 能被硬超时终止并返回 `terminated_reason=timeout`。
  - 危险导入/调用可被策略拦截并返回 `policy_violation`。
  - 直执行端点默认关闭；开启后仍受管理员/DEBUG 保护。
  - CORS 白名单与非白名单行为符合预期。
  - `AUTO_CREATE_TABLES=false` 时不触发 `create_tables()`。
  - 导师活动接口可完成多源合并并按时间倒序。

## 3. 指定回归测试

### 3.1 执行命令
```powershell
.\\.venv-ragtest\\Scripts\\python -m pytest -q `
  backend/tests/test_react_agent_citation_policy.py `
  backend/tests/test_tool_registry_mcp_bridge.py `
  backend/tests/test_tool_registry_mcp_routing.py `
  backend/tests/test_agent_context_budget.py `
  backend/tests/test_agent_function_calling_fallback.py `
  backend/tests/test_internal_mcp_servers.py
```

### 3.2 结果
- 结果：`25 passed`
- 结论：本次改动未破坏 agent/tool 关键链路。

## 4. 前端验证

### 4.1 构建
```powershell
npm --prefix frontend run build
```
- 结果：通过（`tsc -b` + `vite build` 成功）

### 4.2 关键页面 hooks 告警
```powershell
npx --prefix frontend eslint `
  frontend/src/components/layout/MainLayout.tsx `
  frontend/src/pages/chat/ChatPage.tsx `
  frontend/src/pages/dashboard/DashboardPage.tsx `
  frontend/src/pages/knowledge/KnowledgePage.tsx
```
- 结果：通过（0 warning）

### 4.3 全量 lint
```powershell
npm --prefix frontend run lint
```
- 结果：通过（存在 12 条历史 warning，均不在本批关键页面范围）

## 5. 风险与后续建议
- 容器冒烟已执行：
```powershell
docker compose up -d --build backend frontend
docker compose ps
docker compose logs --tail 100 backend
docker compose logs --tail 100 frontend
```
- 结果：
  - `backend/frontend/postgres/redis` 均为 `Up`。
  - 后端日志显示 `AUTO_CREATE_TABLES=false` 已生效，服务正常启动。
- 仍建议在真实交互中补充验证：
  - CodeLab 并发限制触发时前端提示体验
  - 导师动态分页与空态
