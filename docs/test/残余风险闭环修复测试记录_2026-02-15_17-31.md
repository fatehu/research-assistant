# 残余风险闭环修复测试记录

- 时间：2026-02-15 17:31
- 环境：Windows PowerShell + `.venv-ragtest`
- 分支：`feature/acceptance-risk-closure-20260215`

## 测试前置

- 显式注入环境变量（避免 `DATABASE_URL` 为空）：
  - `DATABASE_URL=postgresql://research_user:ci_strong_password_123@localhost:5432/research_assistant`
  - `SECRET_KEY=ci_secret_key_abcdefghijklmnopqrstuvwxyz_123456`
  - `CODELAB_RUNNER_TOKEN=ci_runner_internal_token_123`

## 执行命令与结果

1. CodeLab + 路由前缀 + 限流新增单测
```powershell
cd backend
..\.venv-ragtest\Scripts\python.exe -m pytest tests/test_rate_limit_dependency.py tests/test_codelab_policy_blocklist.py tests/test_codelab_timeout_enforced.py tests/test_codelab_direct_execute_guard.py tests/test_cors_origin_whitelist.py tests/test_tool_architecture_refactor.py -q
```
- 结果：`27 passed`，无失败。

2. broad catch 增量守卫
```powershell
python backend/checks/check_no_new_broad_excepts.py
```
- 结果：`Broad exception guard passed.`

## 中间问题与修复

1. 问题：`test_tool_architecture_refactor.py` 中 WebScrape 相关用例初次失败。
- 现象：`bs4_not_available` 替代预期错误码。
- 原因：文件拆分后 shim 仅 `import *`，导致 monkeypatch 修改的是 shim 命名空间，不是实现模块命名空间。
- 修复：
  - `backend/app/services/notebook_tools.py`
  - `backend/app/services/agent_tools.py`
  - 改为 `sys.modules` 模块转发。
- 复测：`tests/test_tool_architecture_refactor.py` 全通过。

2. 问题：初次测试收集时 `DATABASE_URL` 解析失败。
- 原因：新配置移除弱默认后，测试环境未注入数据库连接串。
- 修复：测试命令前注入显式环境变量。

## 覆盖结论

1. `/api/v1` 前缀切换已在后端主入口、前端 API 客户端与 e2e 脚本落地。
2. 限流功能已具备 429 行为与标准响应头能力。
3. CodeLab runner 通路已打通，且 runner 不可用会返回 503 错误码。
4. 大文件已完成模块迁移（`agent_tools`、`notebook_tools`）。
5. CI 工作流文件已补齐（PR 冒烟 + 夜间全量）。

## 2026-02-15 18:28 补充回归（UI 链路 + 全套闭环）

### 执行命令

```powershell
# 夜间 UI 链路
./e2e/student_role_smoke_e2e.ps1
./e2e/role_route_smoke_e2e.ps1 -Role mentor
./e2e/role_route_smoke_e2e.ps1 -Role admin
./e2e/mcp_settings_login_e2e.ps1

# API 冒烟链路
./acceptance_tests.ps1
./e2e/role_business_api_smoke_e2e.ps1
./e2e/remaining_modules_api_smoke_e2e.ps1

# 核心回归与守卫
.\.venv-ragtest\Scripts\python.exe -m pytest backend/tests/test_rate_limit_dependency.py backend/tests/test_codelab_runner_unavailable.py backend/tests/test_codelab_policy_blocklist.py backend/tests/test_codelab_timeout_enforced.py backend/tests/test_codelab_direct_execute_guard.py backend/tests/test_cors_origin_whitelist.py backend/tests/test_tool_architecture_refactor.py -q
python backend/checks/check_no_new_broad_excepts.py
```

### 结果摘要

1. UI 链路 4/4 通过：student_role、role_route(mentor/admin)、mcp_settings_login。
2. API 链路 3/3 通过：acceptance、role_business、remaining_modules。
3. 核心后端回归：`28 passed`。
4. broad catch 阻断检查：`Broad exception guard passed.`。

### 本轮证据

1. `output/playwright/student-role-smoke-e2e.png`
2. `output/playwright/mentor-role-smoke-e2e.png`
3. `output/playwright/admin-role-smoke-e2e.png`
4. `output/playwright/mcp-settings-login-e2e.png`
5. `output/e2e/remaining_modules_sample_20260215182825836.txt`
6. `output/e2e/invalid_upload_20260215182825836.exe`

### 结论

- 变更已满足“API + UI + 回归守卫”的闭环验收，可进入按 phase 分批提交与推送流程。
