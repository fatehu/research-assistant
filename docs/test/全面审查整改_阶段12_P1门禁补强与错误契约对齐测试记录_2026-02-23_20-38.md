# 全面审查整改 阶段12 测试记录：P1门禁补强与错误契约对齐

## 时间
- 2026-02-23 20:38

## 测试环境
- 分支：`feature/full-remediation-2026-02`
- 后端入口：`http://localhost:8888`
- 前端入口：`http://localhost:3000`

## 测试命令与结果
1. 合约门禁
- 命令：`python backend/checks/check_contract_alignment.py`
- 结果：PASS

2. 合约门禁单测
- 命令：`python -m pytest backend/tests/test_contract_alignment_check.py -q`
- 结果：PASS（5 passed）

3. AppServiceError 契约单测
- 命令：`python -m pytest tests/test_app_service_error_contract.py -q`（workdir=`backend`）
- 结果：PASS（2 passed）

4. 前端静态检查
- 命令：`npm run lint`
- 结果：PASS（14 条既有 warning，未新增 error）

5. 前端构建
- 命令：`npm run build`
- 结果：PASS（既有 chunk warning，未新增构建错误）

6. E2E 回归（受影响脚本）
- 命令：`./e2e/remaining_modules_api_smoke_e2e.ps1`
- 结果：PASS

7. E2E 回归（受影响脚本）
- 命令：`./e2e/role_route_smoke_e2e.ps1 -Role mentor`
- 结果：PASS

8. Broad Exception 门禁回归
- 命令：`python backend/checks/check_no_new_broad_excepts.py`
- 结果：PASS

## 额外说明
- `python -m pytest backend/tests/test_app_service_error_contract.py -q` 在仓库根目录会因 `app` 包路径导致导入失败；
  在 `backend` 目录执行同一测试通过，符合本项目后端测试路径约定。

## 结论
- 本阶段改动已通过目标门禁与关键回归。
- 429 限流场景下脚本具备可恢复能力，减少了并发误报失败风险。

