# 检索性能优化阶段6测试记录

- 记录时间：2026-02-13 17:50
- 测试范围：前端检索参数透传、默认策略、结果元信息展示、相邻上下文展示、后端检索回归稳定性。

## 1. 测试环境
- 系统：Windows（PowerShell）
- 前端目录：`frontend/`
- 后端虚拟环境：`.venv-ragtest`（Python 3.13.3）

## 2. 执行命令与结果

### 2.1 前端 Lint
```bash
npm run lint
```
结果：失败。  
失败原因：项目当前缺少 ESLint 配置文件（`ESLint couldn't find a configuration file`），为仓库既有状态，不是本次改动引入。

### 2.2 前端 Build
```bash
cd frontend
npm run build
```
结果：通过。  
关键输出：`✓ built in 22.26s`。  
备注：存在 Vite 大包告警（历史问题），不影响本次功能正确性。

### 2.3 后端检索回归
```bash
.\\.venv-ragtest\\Scripts\\python -m pytest \
  backend/tests/test_contextual_compression_service.py \
  backend/tests/test_contextual_compression_batch_pipeline.py \
  backend/tests/test_chinese_fts_segmented.py \
  backend/tests/test_query_rewrite_service.py \
  backend/tests/test_vector_search_tuning.py \
  backend/tests/test_contextual_retrieval_window.py \
  backend/tests/test_tool_registry_knowledge_search.py -q
```
结果：通过。  
关键输出：`37 passed in 4.12s`。

## 3. 手工验收点
- 场景A：知识库搜索默认开启压缩，搜索请求可正常返回结果与耗时。
- 场景B：开启相邻上下文后，结果卡片可显示 `adjacent_context` 列表。
- 场景C：结果卡片可显示改写缓存命中、跳过原因、LLM 调用状态。
- 场景D：结果卡片可显示检索维度与压缩回退原因。
- 场景E：`.env` 设置 `VITE_SHOW_RAG_METRICS=true` 时，聊天/Notebook 可显示 RAG 指标。

## 4. 失败与修复记录
- 失败项：前端 build 首次执行报 TypeScript 类型错误（`SearchResultCard.tsx` 相邻上下文数组类型推断不兼容）。
- 修复措施：统一 `AdjacentContextItem` 字段类型，并使用 `NonNullable` 过滤空项。
- 复测结果：`npm run build` 通过。

## 5. 结论
- Phase 6 目标能力已落地：
  - 前端检索体验可感知闭环完成；
  - 默认策略符合“平衡默认”；
  - API 链路能力已透传到前端；
  - 后端关键检索回归全部通过。
- 当前剩余项：`npm run lint` 因仓库缺 ESLint 配置无法通过，需后续单独治理工程化配置。

