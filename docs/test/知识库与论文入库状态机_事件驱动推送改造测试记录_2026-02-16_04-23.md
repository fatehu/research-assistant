# 知识库与论文入库状态机：事件驱动推送改造测试记录（2026-02-16 04:23）

## 1. 测试环境
- OS: Windows 11 + PowerShell
- Python: 3.13（系统环境）
- Frontend: Node 20
- Docker: Docker Desktop + Compose

## 2. 执行命令与结果

### 2.1 后端语法编译检查
```powershell
python -m compileall backend/app/api/knowledge.py backend/app/api/literature.py backend/app/services/status_event_bus.py
```
- 结果：通过。

### 2.2 前端类型与打包
```powershell
cd frontend
npm run build
```
- 结果：通过。
- 备注：仍有历史 chunk size 警告（非本次变更引入）。

### 2.3 现有后端用例尝试（环境依赖记录）
```powershell
pytest -q backend/tests/test_literature_reader_api.py
```
- 结果：失败（环境缺少 `fastapi`，为当前 Python 解释器依赖问题，非本次逻辑失败）。
- 错误：`ModuleNotFoundError: No module named 'fastapi'`

## 3. 关键验证点（人工）
1. 知识库文档上传后：
- 预期：文档状态通过 SSE 自动从 `pending -> processing -> completed/failed` 更新。
- 回退：若 SSE 断开，30 秒兜底轮询仍会更新状态。

2. 论文阅读页加入知识库后：
- 预期：右侧 link 状态自动刷新到 `processing/ready/failed`。
- 回退：若 SSE 断开，30 秒兜底拉取仍可更新。

3. Redis 不可用场景：
- 预期：事件总线降级到本地队列，不阻断主流程。

## 4. 结论
- 本阶段“高频轮询 -> 事件驱动推送”改造已完成，知识库页与论文阅读页均已接入。
- 在保证实时性的同时显著降低了状态查询接口压力，并保留低频兜底增强稳定性。
