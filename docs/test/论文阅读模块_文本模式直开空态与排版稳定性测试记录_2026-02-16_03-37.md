# 论文阅读模块：文本模式直开空态与排版稳定性测试记录（2026-02-16 03:37）

## 1. 测试环境
- OS: Windows 11 + PowerShell
- Frontend: Node 20
- Docker: Docker Desktop + Compose

## 2. 执行命令与结果

### 2.1 前端构建验证
```powershell
cd frontend
npm run build
```
- 结果：通过。
- 说明：TypeScript 编译与 Vite 打包成功。

### 2.2 CI compose 覆盖配置校验
```powershell
docker compose -f docker-compose.yml -f .github/docker-compose.ci.yml config
```
- 结果：通过。
- 说明：CI 覆盖文件可正常解析。

### 2.3 容器同步与健康核验
```powershell
$env:CODELAB_RUNNER_TOKEN='local_dummy_token'
docker compose up -d --build frontend

docker compose ps
docker compose logs --tail 120 backend
docker compose logs --tail 120 frontend
```
- 结果：通过。
- 现象：`backend/frontend` 容器正常启动，日志无新增阻断错误。

## 3. 人工验证要点（本阶段）
1. 直接进入阅读工作台，第一步就切换“文本模式”。
- 预期：不再立即显示“暂无可提取文本”；在文档对象加载后可展示文本。

2. 对双栏论文页查看 `2 / 2.1 / 3.1` 等章节位置。
- 预期：标题与正文不跨列拼接，阅读顺序稳定。

3. 检查长句/编号段落显示。
- 预期：不存在“整段被误放大为标题”或标题层级异常。

## 4. 结论
- 本阶段修复已完成：文本模式直开空态问题已从加载时序层面解决；文本抽取与排版稳定性同步提升；CI 无 GPU 兼容链路已打通。
