# Agent一致性与引用约束测试记录（2026-02-12 22:07）

## 1. 测试目标
验证本次“RAG短板补齐”改动是否生效，重点覆盖：
1. CodeLab / Notebook Agent 的知识库检索接线是否打通。  
2. ReAct 生成阶段是否增加了明确的来源引用约束。  
3. 既有检索增强单测是否保持通过（防回归）。

## 2. 测试环境
1. 系统运行形态：主系统仍以 Docker 为主。  
2. 本地测试工程：使用 Python venv 执行后端单元测试。  
3. venv 路径：`.venv`。

## 3. 测试准备
执行命令：

```powershell
python -m venv .venv
.venv\Scripts\python -m pip install --upgrade pip setuptools wheel
.venv\Scripts\python -m pip install pytest pytest-asyncio pydantic pydantic-settings fastapi sqlalchemy asyncpg psycopg2-binary alembic loguru openai httpx python-dotenv tenacity pgvector redis python-jose passlib bcrypt aiofiles python-multipart email-validator
```

结果：依赖安装成功，无阻断错误。

## 4. 执行用例
执行命令：

```powershell
.venv\Scripts\python -m pytest backend/tests/test_query_rewrite_service.py backend/tests/test_hybrid_retrieval_service.py backend/tests/test_contextual_compression_service.py backend/tests/test_vector_search_tuning.py backend/tests/test_react_agent_citation_policy.py backend/tests/test_tool_registry_knowledge_search.py backend/tests/test_agent_db_wiring.py -q
```

## 5. 测试结果
输出摘要：

```text
......................                                                   [100%]
22 passed in 2.74s
```

结论：本次新增与回归测试全部通过。

## 6. 关键验证点与结论
1. `ToolRegistry` 条件注册验证通过：  
- `db` 有效时包含 `knowledge_search`；  
- `db=None` 时不包含 `knowledge_search`。

2. Agent 路由接线验证通过：  
- codelab Agent 聊天路由内部已使用 `db=db` 初始化 `ToolRegistry`；  
- notebook_agent 关键路由包含 `db` 依赖参数。

3. 引用约束验证通过：  
- ReAct 系统提示词包含“知识检索引用规范”；  
- `knowledge_search` 观测结果回写文案包含 `[来源X]` 强约束。

4. 检索增强回归通过：  
- Query Rewrite / Hybrid RRF / Contextual Compression / HNSW 调参相关单测均通过。

## 7. 风险与后续建议
1. 当前为单元测试与静态接线验证，尚未做端到端对话场景的自动化验收（例如“真实对话输出引用率”统计）。  
2. 建议下一步增加 E2E 回归：
- AI 对话 Agent 使用知识库问答，断言回答含 `[来源X]`；  
- CodeLab Agent 触发 `knowledge_search` 后断言回答带来源引用。

