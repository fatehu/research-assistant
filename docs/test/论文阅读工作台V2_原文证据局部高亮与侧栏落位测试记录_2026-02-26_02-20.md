# 测试记录：原文证据局部高亮与侧栏落位修复

## 测试时间
- 2026-02-26 02:20（本地）

## 环境
- OS: Windows（PowerShell）
- 前端: Vite + React
- 后端: FastAPI（Docker）
- 容器:
  - `research_frontend`（3000）
  - `research_backend`（8888）

## 执行项
1. 前端构建验证
   - 命令：`cd frontend && npm run build`
   - 结果：通过
2. 后端 compose 相关测试
   - 命令：`pytest backend/tests/test_literature_reader_composed.py -q`
   - 结果：17 passed
3. 容器状态核验
   - 命令：`docker compose ps backend frontend`
   - 结果：两服务均 Up
4. 服务可用性核验
   - 命令：`Invoke-WebRequest http://localhost:8888/docs`
   - 结果：200
5. 乱码巡检（代码侧）
   - 命令：`rg -n "锟|闂|宸蹭粠|鏀惰|澶嶅埗|淇|闄嶇骇" backend frontend`
   - 结果：代码侧未命中（仅历史 debug 日志文件命中）

## 结果
1. 已移除遮挡正文的悬浮证据层。
2. 证据展示位置调整为右侧批注区下方。
3. 证据预览支持 PDF 局部高亮图与定位依据/置信度展示。
4. 后端 compose 乱码标签已清理。

## 已知限制
1. 当前无法在本地测试账号直接复现 `paper_id=78`（该 ID 对测试账号返回 404）。
2. 你账号下的 `http://localhost:3000/literature/78/read` 仍需你侧做最终 UI 体验验收（我已给出重点验收项）。

