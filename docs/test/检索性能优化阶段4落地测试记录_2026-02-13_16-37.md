# 检索性能优化阶段4落地测试记录

- 记录时间：2026-02-13 16:37
- 范围：维度守卫、回归测试、容器评测门槛复核。

## 环境
- 本地：`.venv-ragtest`（Python 3.13.3）
- 容器：`research_backend`（PostgreSQL + Alembic）

## 一、语法校验
```bash
.\\.venv-ragtest\\Scripts\\python.exe -m py_compile \
  backend/app/api/knowledge.py \
  backend/app/services/agent_tools.py \
  backend/app/maintenance/backfill_context_summary_and_embeddings.py
```
结果：通过。

## 二、阶段回归测试
```bash
.\\.venv-ragtest\\Scripts\\python.exe -m pytest \
  backend/tests/test_contextual_compression_service.py \
  backend/tests/test_contextual_compression_batch_pipeline.py \
  backend/tests/test_chinese_fts_segmented.py \
  backend/tests/test_query_rewrite_service.py \
  backend/tests/test_vector_search_tuning.py \
  backend/tests/test_tool_registry_knowledge_search.py \
  backend/tests/test_contextual_retrieval_window.py -q
```
结果：`37 passed in 3.94s`。

## 三、容器评测门槛复核（阶段4）
```bash
docker exec research_backend python -m app.maintenance.eval_retrieval_recall \
  --cases tests/data/retrieval_eval_cases.v1.json \
  --dims 1024,512 \
  --top-k 10 \
  --baseline-dim 1024 \
  --target-dim 512 \
  --gate-max-recall-drop-pct 3 \
  --gate-min-latency-improve-pct 15 \
  --latency-corpus-multiplier 20 \
  --score-repeat 5 \
  --output /app/docs/test/阶段4检索评测报告_2026-02-13_16-27.md
```
结果摘要：
- `pass_recall=True`
- `pass_latency=True`
- `pass=True`
- 报告已落地：`docs/test/阶段4检索评测报告_2026-02-13_16-27.md`

## 四、结论
- 阶段4已从“可评测”推进到“可上线守卫”：在线检索按维度隔离，避免混维误检索。
- 前端接口未新增必填参数，现有调用兼容。
