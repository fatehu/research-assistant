# 阶段3 CodeLab Runner沙箱化测试补档

- 时间：2026-02-15 20:08
- 对应提交：`4e896ae feat(codelab): 引入独立 runner 沙箱执行链路`
- 目的：补齐阶段提交对应的 `docs/test` 证据，满足 `scripts/code.md` 要求。

## 测试环境

1. Docker：`backend + codelab-runner + redis + postgres + frontend`
2. Python 测试环境：`.venv-ragtest`

## 测试命令（回放）

```powershell
.\.venv-ragtest\Scripts\python.exe -m pytest backend/tests/test_codelab_runner_unavailable.py backend/tests/test_codelab_policy_blocklist.py backend/tests/test_codelab_timeout_enforced.py backend/tests/test_codelab_direct_execute_guard.py -q
docker compose ps
docker compose logs --tail 100 codelab-runner
```

## 结果

1. runner 不可用场景返回 503，错误码可观测。
2. policy blocklist、超时中断、direct execute guard 通过回归。
3. 容器日志可定位执行异常，不存在静默吞错。

## 结论

- 阶段3实现满足“独立 runner 沙箱执行 + 故障可观测”的验收要求。
