# 检索稳定性阶段2测试记录
时间：2026-02-13 19:27

## 1. 测试环境
- 系统：Windows + Docker Desktop
- 后端容器：`research_backend`
- 数据库容器：`research_postgres`
- Python 环境：`.venv-ragtest`

## 2. 关键问题复现
- 现象：上传 `deepseek-ocr.pdf` 后持续 `processing`。
- 根因日志：
  - `处理文档失败 83: 'embedding_dimension' is an invalid keyword argument for DocumentChunk`
- 结论：代码写入了 `embedding_dimension`，但 `DocumentChunk` 模型缺少该字段，且数据库表也缺列。

## 3. 执行命令
- 语法检查：
  - `.\\.venv-ragtest\\Scripts\\python -m py_compile backend/app/models/knowledge.py backend/alembic/versions/012_document_chunks_embedding_dimension.py backend/app/api/knowledge.py`
- 单测：
  - `.\\.venv-ragtest\\Scripts\\python -m pytest backend/tests/test_document_chunk_model_schema.py backend/tests/test_document_status_guard_service.py backend/tests/test_tool_registry_knowledge_search.py -q`
- 检索关键回归：
  - `.\\.venv-ragtest\\Scripts\\python -m pytest backend/tests/test_contextual_compression_service.py backend/tests/test_contextual_compression_batch_pipeline.py backend/tests/test_chinese_fts_segmented.py backend/tests/test_query_rewrite_service.py backend/tests/test_vector_search_tuning.py backend/tests/test_contextual_retrieval_window.py backend/tests/test_tool_registry_knowledge_search.py backend/tests/test_document_status_guard_service.py backend/tests/test_document_chunk_model_schema.py -q`
- 迁移：
  - `docker exec research_backend sh -lc "cd /app && alembic upgrade head"`
- 端到端：
  - `powershell -ExecutionPolicy Bypass -File e2e/knowledge_upload_pipeline_e2e.ps1 -BackendBaseUrl http://localhost:8888 -MaxWaitSeconds 600`

## 4. 结果
- 单测：`10 passed`
- 回归：`43 passed`
- 数据库结构校验：
  - `document_chunks.embedding_dimension` 已存在，`NOT NULL`，默认值 `1024`
- 端到端：通过，输出 `E2E success: upload -> processing -> search passed.`
- 线上重试：
  - `doc_id=83` 已成功处理完成，`chunk_count=139`

## 5. 失败与修复
- 失败点：模型字段与业务写入参数不一致。
- 修复：
  - 模型补字段；
  - 新增迁移补列并回填；
  - 新增模型契约测试；
  - 新增上传处理端到端脚本。

## 6. 结论
- 该问题已被修复且被端到端回归覆盖。
- 后续若字段再发生模型/迁移不一致，`test_document_chunk_model_schema` 与 `knowledge_upload_pipeline_e2e.ps1` 可第一时间暴露问题。
