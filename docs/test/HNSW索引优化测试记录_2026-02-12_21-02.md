# HNSW 索引优化测试记录（2026-02-12）
- 记录时间：2026-02-12 21:02
- 变更主题：pgvector HNSW 并发索引 + 检索 `ef_search` 参数调优

## 1. 测试环境
- Docker Compose 容器环境
- 服务状态：
  - `research_backend`：Up
  - `research_postgres`：healthy
  - `research_redis`：healthy

## 2. 测试项与结果
### 2.1 语法检查
- 命令：
```bash
docker compose exec -T backend sh -lc "cd /app && PYTHONPATH=/app python -m py_compile app/services/vector_search_tuning.py app/api/knowledge.py app/services/agent_tools.py app/config.py alembic/versions/009_hnsw_index_concurrently.py tests/test_vector_search_tuning.py"
```
- 结果：通过

### 2.2 单元测试
- 命令：
```bash
docker compose exec -T backend sh -lc "cd /app && PYTHONPATH=/app pytest tests/test_vector_search_tuning.py tests/test_query_rewrite_service.py tests/test_hybrid_retrieval_service.py -q"
```
- 结果：通过
  - `12 passed, 6 warnings in 1.49s`
  - warning 为 `pytest-asyncio` 的 `event_loop` fixture 弃用提示，非本次功能缺陷

### 2.3 Alembic 迁移执行验证
- 命令：
```bash
docker compose exec -T backend sh -lc "cd /app && alembic upgrade head"
```
- 结果：通过
  - 执行记录：`Running upgrade 008_embedding_dimension -> 009_hnsw_index_concurrently`

### 2.4 索引落库验证
- 命令：
```bash
docker compose exec -T postgres psql -U yui -d research_assistant -c "SELECT indexname, indexdef FROM pg_indexes WHERE tablename='document_chunks' ORDER BY indexname;"
```
- 结果：通过
  - 检测到索引：`ix_chunks_embedding_hnsw`
  - 索引定义包含：`USING hnsw (embedding vector_cosine_ops) WITH (m='16', ef_construction='64')`

## 3. 结论
- 并发 HNSW 索引迁移可执行，且已正确创建目标索引。
- 两条检索路径（知识库搜索 API / Agent 工具）已接入 `hnsw.ef_search` 设置逻辑。
- 当前改动通过容器环境语法检查、单测与迁移验证。
