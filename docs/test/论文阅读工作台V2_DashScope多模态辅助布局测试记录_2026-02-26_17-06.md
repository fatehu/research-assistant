# 论文阅读工作台V2：DashScope 多模态辅助布局测试记录

- 时间：2026-02-26 17:06
- 环境：Windows + PowerShell + `.venv-ragtest` + Docker Compose

## 一、静态检查
1. 命令：
   - `python -m py_compile backend/app/services/reader_multimodal_layout_service.py backend/app/services/literature_reader_service.py backend/app/services/literature_reader_compose_service.py backend/app/schemas/literature.py backend/app/api/literature.py`
2. 结果：通过。

## 二、后端测试
1. 命令：
   - `.\.venv-ragtest\Scripts\python.exe -m pytest -q backend/tests/test_literature_reader_composed.py`
2. 结果：`21 passed`。
3. 备注：存在已知依赖告警（pytest-asyncio、pydantic deprecation），不影响本次功能正确性。

## 三、前端构建
1. 命令：
   - `npm run build`（目录：`frontend`）
2. 结果：构建成功。
3. 备注：Vite 给出 chunk size 警告，属于性能优化项，不影响功能上线。

## 四、容器同步与健康核验
1. 命令：
   - `docker compose up -d --build backend frontend`
   - `docker compose ps`
   - `docker compose logs --tail 100 backend`
   - `docker compose logs --tail 100 frontend`
2. 结果：
   - `backend/frontend/postgres/redis` 均正常启动。
   - 后端启动日志正常，应用完成 startup。
   - 前端 dev 服务在 `http://localhost:3000` 正常监听。

## 五、乱码巡检
1. 命令：
   - `chcp 65001`
   - `Get-Content -Encoding utf8 scripts/code.md`
   - `rg` 对本次改动文件执行异常字符巡检。
2. 结果：本次新增与改动内容未发现乱码。

## 六、结论
1. 本阶段“非 OCR 多模态辅助布局”已按计划接入。
2. 主链路可在低置信页触发视觉裁决，并在失败时自动降级。
3. 三通道分流、目录治理、质量指标扩展均可用。
