# CodeLab轻量中文字体修复测试记录（2026-02-16 17:29）

## 1. 测试环境
- 系统：Windows（本地开发机）
- 容器：Docker Desktop 4.41.2
- 服务：`backend`、`codelab-runner`、`frontend`、`postgres`、`redis`

## 2. 测试目标
- 验证轻量字体方案是否能稳定支持中文绘图。
- 验证修复后日志是否无缺字告警噪声。

## 3. 测试命令与结果
1. 字体存在性检查
- 命令：
```bash
docker compose exec codelab-runner sh -lc "fc-list | grep -Ei 'WenQuanYi Zen Hei' | head -n 3"
```
- 结果：返回 `WenQuanYi Zen Hei` 相关字体记录，判定通过。

2. 代码执行链路检查（中文图标题）
- 命令：通过 `backend` 调用 `codelab-runner` 内部接口 `/internal/codelab/execute`，提交中文标题 matplotlib 绘图代码。
- 关键结果：
  - `status 200`
  - `success True`
  - `output_count 1`
- 判定：执行成功且产出图像输出。

3. 日志检查
- 命令：
```bash
docker compose logs --tail 120 codelab-runner
```
- 结果：无新增 `Glyph ... missing from current font` 告警。

## 4. 失败与修复
- 失败现象：初次接口调用返回 `422`。
- 原因：请求体字段不匹配（误用 `kernel_id`，实际需要 `notebook_id`）。
- 修复：按 `ExecutePayload` 改为 `notebook_id` 后通过。

## 5. 结论
- 轻量字体方案生效，中文绘图能力恢复且稳定。
- 本阶段改造满足上线前验证标准，可进入下一阶段配置同步与提交流程。
