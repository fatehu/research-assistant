# RAG质量观测基线增强测试记录（2026-02-13 00:04）

## 1. 测试目标
验证第 7 项“RAG 质量观测基线”改动是否生效，并确保既有检索增强链路无回归。

## 2. 覆盖范围
1. `ReActAgent` 是否在 `done` 事件输出 `rag_metrics`。
2. `rag_metrics` 关键字段是否正确统计：
- `knowledge_search_calls`
- `source_labels_count`
- `citation_valid`
- 压缩与引用修复相关计数
3. `chat/codelab/notebook_agent` 是否透传 `rag_metrics`。
4. 既有 rewrite/hybrid/compression/vector/citation/tool wiring 测试是否全部通过。

## 3. 执行命令

```powershell
.venv\Scripts\python -m pytest backend/tests/test_react_agent_citation_policy.py backend/tests/test_agent_db_wiring.py backend/tests/test_tool_registry_knowledge_search.py -q
```

```powershell
.venv\Scripts\python -m pytest backend/tests/test_query_rewrite_service.py backend/tests/test_hybrid_retrieval_service.py backend/tests/test_contextual_compression_service.py backend/tests/test_vector_search_tuning.py backend/tests/test_react_agent_citation_policy.py backend/tests/test_tool_registry_knowledge_search.py backend/tests/test_agent_db_wiring.py -q
```

## 4. 结果
1. 第一轮：`18 passed, 3 warnings`
2. 第二轮（完整 RAG 关键回归）：`34 passed, 3 warnings`

## 5. 关键验证点
1. `test_run_done_event_contains_rag_metrics_baseline`：验证脚本化两轮 ReAct 后 `done.data.rag_metrics` 存在且数值正确。
2. `test_chat_stream_done_payload_includes_rag_metrics`：验证 Chat 路由 done 透传。
3. `test_codelab_done_payload_includes_rag_metrics`：验证 CodeLab 路由 done 透传。
4. `test_notebook_agent_done_payload_includes_rag_metrics`：验证 Notebook Agent 路由 done 透传。

## 6. 结论
第 7 项第一阶段已落地：
1. 已建立统一 RAG 质量观测字段并打通三条流式链路。
2. 已建立可重复执行的最小 E2E 质量基线测试。
3. 现有 RAG 核心回归测试全部通过。

## 7. 备注
warnings 为现有 Pydantic V2 兼容提示，不影响本次结论。
