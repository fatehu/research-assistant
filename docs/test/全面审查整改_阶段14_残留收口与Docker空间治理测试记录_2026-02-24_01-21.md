# 全面审查整改 阶段14：残留收口与 Docker 空间治理测试记录

- 时间：2026-02-24 01:21
- 环境：Windows PowerShell + Docker Desktop
- 分支：feature/full-remediation-2026-02

## 一、Docker 空间治理证据

### 1) 清理前
- `docker system df`
  - Images: 15 / 24.4GB（可回收 23.41GB）
  - Build Cache: 32.59MB（可回收 32.59MB）

### 2) 执行命令
- `docker builder prune -af`
- `docker image prune -af`
- `docker container prune -f`

### 3) 清理结果
- `docker image prune -af` 输出：`Total reclaimed space: 17.38GB`
- 清理后重建并拉起项目容器：
  - `docker compose up -d --build backend frontend`
  - `docker compose ps` 显示 backend/frontend/postgres/redis/codelab-runner 均 Up

## 二、残留问题复现与修复验证

### 1) 复现（修复前）
- 命令：`docker compose run --rm backend python -m pytest tests/test_agent_db_wiring.py -q`
- 结果：失败
- 失败点：
  - `module 'app.api.codelab' has no attribute 'notebook_agent_chat'`
  - metadata 字符串断言不匹配

### 2) 修复后回归
- 命令：
  - `docker compose run --rm backend python -m pytest tests/test_agent_db_wiring.py -q`
  - `docker compose run --rm backend python -m pytest tests/test_agent_core_unified_routes.py -q`
  - `docker compose run --rm backend python -m pytest tests/test_smart_chunking.py -q`
- 结果：
  - `test_agent_db_wiring.py`：13 passed
  - `test_agent_core_unified_routes.py`：2 passed
  - `test_smart_chunking.py`：34 passed

### 3) 门禁校验（宿主机）
- `python backend/checks/check_no_new_broad_excepts.py`：PASS
- `python backend/checks/check_contract_alignment.py`：PASS

## 三、结论

- Docker “只增不减”问题已通过缓存/镜像/停止容器清理得到实质缓解（回收约 17.38GB）。
- 后端残留失败已收口，核心兼容问题修复后关键测试通过。
- 当前可继续推进后续全量回归或进入发布前收尾。
