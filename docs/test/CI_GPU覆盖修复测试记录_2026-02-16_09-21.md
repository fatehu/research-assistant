# CI GPU 覆盖修复测试记录（2026-02-16 09:21）

## 1. 测试环境
- 分支：`feature/ci-gpu-reset-fix-20260216`
- 系统：Windows + Docker Desktop
- 目标：验证 CI 组合配置下 backend 不再要求 GPU

## 2. 测试命令
```powershell
$env:CODELAB_RUNNER_TOKEN='ci_runner_internal_token_1234567890'
docker compose -f docker-compose.yml -f .github/docker-compose.ci.yml config | Select-String -Pattern "gpus|count"

docker compose -f docker-compose.yml -f .github/docker-compose.ci.yml up -d --build backend frontend postgres redis codelab-runner

docker compose -f docker-compose.yml -f .github/docker-compose.ci.yml ps

docker compose -f docker-compose.yml -f .github/docker-compose.ci.yml logs --tail 80 backend
docker compose -f docker-compose.yml -f .github/docker-compose.ci.yml logs --tail 50 frontend
```

## 3. 结果
- `config` 检查未出现 `backend.gpus`。
- `up -d --build` 成功完成，backend/frontend/codelab-runner 均正常启动。
- backend 日志正常完成 Alembic + Uvicorn 启动。
- 前端日志正常启动 Vite。

## 4. 结论
- 修复有效：CI 组合配置已可在无 GPU 环境启动，不再触发 GPU capability 报错。
