# 论文阅读模块四周方案阶段收口测试记录

- 时间：2026-02-16 00:41
- 分支：`feature/code-md-doc-closure-20260215`
- 环境：Windows 11 + PowerShell（UTF-8）+ `.venv-ragtest` + Docker Desktop

## 1. 测试命令

1. 后端语法编译

```powershell
python -m py_compile backend/app/api/literature.py backend/app/schemas/literature.py backend/app/models/literature.py backend/alembic/versions/016_literature_reader_and_qa.py
```

2. 文献相关定向回归

```powershell
.\.venv-ragtest\Scripts\python -m pytest backend/tests/test_tool_architecture_refactor.py -k literature_service_multi_deduplicates_and_keeps_better_paper -q
Set-Location backend
$env:DATABASE_URL='postgresql://postgres:postgres@127.0.0.1:5432/postgres'
..\.venv-ragtest\Scripts\python -m pytest tests/test_tool_architecture_refactor.py -k literature_search_multi_and_default_source_compatible -q
```

3. 前端构建校验

```powershell
npm --prefix frontend run build
```

4. 容器同步与健康核验

```powershell
docker compose up -d --build backend frontend
docker compose ps
docker compose logs --tail 100 backend
docker compose logs --tail 100 frontend
```

## 2. 测试结果

- `py_compile`：通过。
- 两项定向 `pytest`：均通过（各 `1 passed`）。
- 前端 `build`：通过（仅有 chunk 体积告警，无编译错误）。
- Docker 同步：通过，`backend/frontend` 均成功启动，日志显示应用启动完成。

## 3. 失败与修复

- 本轮收口测试无新增失败。
- 仍需注意：`alembic upgrade head` 依赖本地数据库凭据，若凭据错误会导致环境级失败，不属于代码逻辑回归。

## 4. 结论

- 本轮改动已满足“代码可编译、关键链路定向回归通过、容器已同步并健康”要求。
- 可进入 PR 评审阶段，并继续补充端到端手测（入库完成后询问与引用回跳）。
