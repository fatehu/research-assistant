# 检索性能优化阶段7测试记录

- 记录时间：2026-02-13 21:00
- 测试目标：验证“弹性维度策略 + 长查询可见性闭环”在 API、Agent Tool、前端构建层面可用且无回归。

## 1. 测试环境
- 系统：Windows（PowerShell）
- 前端目录：`frontend/`
- 后端虚拟环境：`.venv-ragtest`（Python）

## 2. 执行命令与结果
1. 前端静态检查
```bash
npm run lint
```
结果：通过（0 error，存在历史 Hook warning，不阻断）。

2. 前端构建
```bash
npm run build
```
结果：通过。关键输出：`built in 19.16s`。

3. 后端语法校验（本阶段改动文件）
```bash
python -m py_compile backend/app/config.py backend/app/models/knowledge.py backend/app/api/knowledge.py backend/app/services/agent_tools.py backend/app/services/embedding_service.py backend/app/services/embedding_dimension_policy_service.py backend/app/services/dimension_rebuild_service.py backend/tests/test_embedding_dimension_policy_service.py backend/tests/test_tool_registry_knowledge_search.py
```
结果：通过。

4. 后端关键回归
```bash
.\\.venv-ragtest\\Scripts\\python -m pytest backend/tests/test_embedding_dimension_policy_service.py backend/tests/test_tool_registry_knowledge_search.py backend/tests/test_contextual_compression_service.py backend/tests/test_contextual_compression_batch_pipeline.py backend/tests/test_chinese_fts_segmented.py backend/tests/test_query_rewrite_service.py backend/tests/test_vector_search_tuning.py backend/tests/test_contextual_retrieval_window.py -q
```
结果：通过。关键输出：`41 passed in 4.59s`。

## 3. 手工验收点
1. 长查询可见性
- 搜索中可看到阶段提示与已耗时秒数。
- 支持“取消搜索”。

2. 超时降级重试
- 主请求超时后自动关闭 rewrite/reranker/compression 进行重试。
- 结果区出现“降级结果”提示。
- 卡片元信息可显示 `fallback_retry_used / fallback_retry_reason`。

3. 双链路一致性
- API 与 Agent Tool 都按 `(embedding_model, embedding_dimension)` 分组检索并融合。
- 未出现“只改 API 未改 Agent”的能力分叉。

## 4. 失败与修复
1. 问题：早前一次文件写入导致部分文件编码损坏（文本异常）。
2. 修复：先回退受影响文件到 HEAD，再使用 `apply_patch` 逐步重放 Phase 7 改动，并补充语法与回归测试确认。
3. 复测结论：本记录中的所有命令均通过。

## 5. 结论
- 本阶段功能已打通并通过关键验证。
- 满足“弹性维度自动化”和“长查询前端可感知”的上线前验收要求。
