# 检索稳定性阶段1测试记录
时间：2026-02-13 18:27

## 1. 测试环境
- 系统：Windows + Docker Desktop
- 代码分支：`main`
- 后端容器：`research_backend`
- 数据库容器：`research_postgres`
- Python 环境：`.venv-ragtest`

## 2. 执行命令
- 语法校验：
  - `.\\.venv-ragtest\\Scripts\\python -m py_compile backend/app/api/knowledge.py backend/app/config.py backend/app/services/document_status_guard_service.py`
- 单测：
  - `.\\.venv-ragtest\\Scripts\\python -m pytest backend/tests/test_document_status_guard_service.py backend/tests/test_contextual_retrieval_window.py -q`
  - `.\\.venv-ragtest\\Scripts\\python -m pytest backend/tests/test_contextual_compression_service.py backend/tests/test_contextual_compression_batch_pipeline.py backend/tests/test_chinese_fts_segmented.py backend/tests/test_query_rewrite_service.py backend/tests/test_vector_search_tuning.py backend/tests/test_contextual_retrieval_window.py backend/tests/test_tool_registry_knowledge_search.py backend/tests/test_document_status_guard_service.py -q`
- 线上问题复核（数据库）：
  - `SELECT ... FROM documents WHERE id IN (81,82);`
- 历史陈旧任务止血（一次性）：
  - `UPDATE documents SET status='failed' ... WHERE status IN ('pending','processing') AND COALESCE(updated_at, created_at) < NOW() - INTERVAL '2 hours';`

## 3. 测试结果
- 语法校验：通过。
- 单测结果：
  - 首轮：9 passed
  - 回归集合：41 passed
- 线上复核：
  - `doc_id=82` 已由状态接口自动转为 `failed`，错误文案为超时提示。
  - 批量止血后：`doc_id=81` 及一条更早历史文档也已转 `failed`。

## 4. 失败与修复
- 失败现象：编辑 `backend/app/api/knowledge.py` 时因编码敏感段导致临时语法损坏。
- 修复措施：恢复文件到 `HEAD`，改为最小差异补丁方式插入守卫逻辑，避免再次触碰大段编码内容。
- 复验：`py_compile` 与完整回归测试全部通过。

## 5. 结论
- “旧文档长期处理中 + 后端持续 status 轮询”问题已闭环。
- 新增守卫可防止同类问题再次长期暴露到前端。
- 当前部署下，用户在文档列表可直接感知为“处理失败（超时）”，轮询会自然停止。
