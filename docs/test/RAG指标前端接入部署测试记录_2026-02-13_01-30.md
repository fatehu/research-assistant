# RAG指标前端接入部署测试记录（2026-02-13 01:30）

## 1. 测试目标
验证以下内容在 Docker 运行形态下是否生效：
1. 全栈容器可稳定重建启动。
2. 后端健康接口可用。
3. Chat / Notebook Agent 流式 `done` 事件包含 `rag_metrics`。
4. 前端开关 `VITE_SHOW_RAG_METRICS` 在容器中可透传（false/true 两种场景）。

## 2. 执行步骤与命令
### 2.1 全栈重建启动（保留数据卷）
```powershell
docker compose down --remove-orphans
docker compose up -d --build postgres redis backend frontend
docker compose ps
docker compose logs backend --tail 200
```

### 2.2 基础健康检查
```powershell
Invoke-WebRequest http://localhost:8888/health
Invoke-WebRequest http://localhost:8888/health/config
Invoke-WebRequest http://localhost:3000
```

### 2.3 严格验收：SSE done.rag_metrics
执行了 Python 脚本（基于 httpx）：
1. 自动注册测试用户并获取 token。
2. 调用 `/api/chat/send`（stream=true）并抓取 `done`。
3. 创建 notebook 并调用 `/api/codelab/notebooks/{id}/agent/chat`（stream=true）抓取 `done`。
4. 校验 `rag_metrics` 存在且包含关键字段：
- `knowledge_search_calls`
- `source_labels_count`
- `citation_valid`
- `compression_success_chunks`
- `compression_fallback_chunks`

### 2.4 开关场景验证（false/true）
```powershell
$env:VITE_SHOW_RAG_METRICS='false'; docker compose up -d --build frontend; docker exec research_frontend printenv VITE_SHOW_RAG_METRICS
$env:VITE_SHOW_RAG_METRICS='true';  docker compose up -d --build frontend; docker exec research_frontend printenv VITE_SHOW_RAG_METRICS
```

## 3. 测试结果
1. 容器状态：`postgres/redis/backend/frontend` 均 `Up`，其中 `postgres/redis` 为 `healthy`。
2. 健康接口：
- `GET /health` 返回可用（database=healthy）。
- `GET /health/config` 返回配置结构。
- 前端 `http://localhost:3000` 返回 200。
3. SSE 严格验收结果：
- Chat `done`：包含 `rag_metrics`。
- Notebook Agent `done`：包含 `rag_metrics`。
- 自动校验输出：`STRICT_ACCEPTANCE_RAG_METRICS=PASS`。
4. 开关透传：
- 设置 `false` 后容器内环境变量为 `false`。
- 设置 `true` 后容器内环境变量为 `true`。

## 4. 关键观测样例
1. Chat done keys：`answer/message_id/rag_metrics/react_steps/thought`
2. Notebook done keys：`code_blocks/rag_metrics/type`
3. 本轮样例里 `knowledge_search_calls=1`，说明工具链已触发知识检索。

## 5. 结论
本次“前端接入 RAG 质量指标 + Docker 透传 + 全栈重建部署”已完成，严格验收通过。

## 6. 备注
仓库存在历史 TypeScript 报错基线，本次不以 `npm run build` 作为发布阻断条件；以 Docker 运行态与链路验收结果作为准入结论。
