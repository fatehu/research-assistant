# 检索性能优化阶段4测试记录

- 记录时间：2026-02-13 15:41
- 阶段：Phase 4（提交 4）

## 测试环境
1. 本机 venv：`.venv-ragtest`
2. Docker：`research_backend`、`research_postgres`

## 执行记录
1. 向量调优与回归测试（venv）：

```bash
.\.venv-ragtest\Scripts\python -m pytest backend/tests/test_vector_search_tuning.py backend/tests/test_tool_registry_knowledge_search.py -q
```

结果：`8 passed in 1.00s`

2. 阶段评测（docker）：

```bash
docker exec research_backend python -m app.maintenance.eval_retrieval_recall \
  --cases /app/tests/data/retrieval_eval_cases.v1.json \
  --top-k 10 \
  --dims 1024,512 \
  --score-repeat 128 \
  --latency-corpus-multiplier 16 \
  --baseline-dim 1024 \
  --target-dim 512 \
  --gate-max-recall-drop-pct 3 \
  --gate-min-latency-improve-pct 15 \
  --fail-on-gate \
  --output /app/docs/test/阶段4检索评测报告_2026-02-13_15-38.md
```

结果：门槛通过（exit code 0）

## 指标结果
1. 1024：`mean_recall@10=0.6`，`mean_mrr=0.6`，`mean_retrieval_latency_ms=1.0477`
2. 512：`mean_recall@10=0.6`，`mean_mrr=0.6`，`mean_retrieval_latency_ms=0.2294`
3. 严格门槛：
   - `recall_drop_pct=0.0%`（通过）
   - `latency_improve_pct=78.1044%`（通过）

## 产物
1. 评测报告：`docs/test/阶段4检索评测报告_2026-02-13_15-38.md`
2. 评测数据：`backend/tests/data/retrieval_eval_cases.v1.json`

## 问题与修复
1. 初次执行出现 BOM 解析错误：已改为 `utf-8-sig` 读取。
2. 初次延迟门槛失败（顺序偏置）：已修复为按输入顺序执行并加入 warm-up。
