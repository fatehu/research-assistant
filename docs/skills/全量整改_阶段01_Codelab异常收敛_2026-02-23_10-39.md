# 全量整改阶段01：CodeLab 宽异常收敛实施说明

- 时间：2026-02-23 10:39
- 阶段目标：将 `backend/app/api/codelab.py` 的 `except Exception` 数量压回 CI 基线，先恢复 `check_no_new_broad_excepts` 门禁。
- 关联 commit：`fix(codelab): 收敛宽异常并修复门禁超线`

## 本阶段改动
1. 在 `backend/app/api/codelab.py` 中，将两个“迭代次数字符串转整数”分支的捕获范围由 `except Exception` 收紧为 `except (TypeError, ValueError)`。
2. 该修改不改变正常业务流程，仅避免过宽异常吞噬并满足基线策略。

## 设计取舍
1. 本阶段采用最小改动策略，只处理门禁超线点，不在此 commit 混入结构重构，确保可快速回归与回滚。
2. 类型转换失败属于可预期输入异常，使用 `TypeError/ValueError` 足够覆盖，且比 `Exception` 更精确。

## 风险与回滚
1. 风险：极低。仅影响迭代字段解析失败时的 fallback 路径。
2. 回滚：可直接回滚本 commit，不影响数据库与外部接口。

## 效果感知
1. `python backend/checks/check_no_new_broad_excepts.py` 从失败（13>11）恢复为通过。
2. 后续阶段可在此基础上继续做大文件拆分与异常治理。
