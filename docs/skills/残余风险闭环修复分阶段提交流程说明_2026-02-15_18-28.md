# 残余风险闭环修复分阶段提交流程说明

- 时间：2026-02-15 18:28
- 分支：`feature/acceptance-risk-closure-20260215`
- 目标：在不混入无关改动的前提下，按 `code.md` 将本次闭环改造拆分为可审阅、可回滚的多条中文提交。

## 本阶段执行策略

1. 先完成全套验证（API 冒烟 + UI 链路 + 核心回归 + 守卫脚本）。
2. 先补齐文档证据（`docs/skills` + `docs/test`），再进行分阶段提交。
3. 每条提交只包含一个主题，提交前执行：
   - `git status`
   - `git diff`
   - `git diff --cached`

## 建议提交顺序（按 phase）

1. Phase 1：配置与凭据硬化
   - `backend/app/config.py`
   - `docker-compose.yml`
   - `.env.example`
   - `README.md`
2. Phase 2：`/api/v1` 切换与限流落地
   - `backend/app/main.py`
   - `backend/app/core/rate_limit.py`
   - 前端 API 调用改造文件
   - `acceptance_tests.ps1` 与相关 `e2e` API 脚本
3. Phase 3：CodeLab runner 沙箱化
   - `backend/app/sandbox_runner/*`
   - `backend/app/services/codelab_executor.py`
   - `backend/app/api/codelab.py`
   - `docker-compose.yml` 中 runner 服务部分
4. Phase 4：异常治理与阻断守卫
   - `backend/app/core/errors.py`
   - `backend/app/core/error_handlers.py`
   - `backend/checks/check_no_new_broad_excepts.py`
5. Phase 5：大文件拆分
   - `backend/app/services/agent_tools.py`
   - `backend/app/services/notebook_tools.py`
   - `backend/app/services/agent_tools_impl/*`
   - `backend/app/services/notebook_tools_impl/*`
6. Phase 6：CI 工作流
   - `.github/workflows/pr-api-smoke.yml`
   - `.github/workflows/nightly-e2e-full.yml`
7. 回归测试与文档收口
   - 相关测试文件更新
   - `docs/test/残余风险闭环修复测试记录_2026-02-15_17-31.md`
   - 本文档与补充测试文档

## 结果判定标准

1. 各提交可独立解释“改了什么、为什么改”。
2. 不包含用户既有未纳入本次范围的临时文件或历史文档。
3. 推送后可直接用于 PR 审阅。
