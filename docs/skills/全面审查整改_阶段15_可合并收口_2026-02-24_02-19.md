# 全面审查整改 阶段15：可合并收口实施说明

- 时间：2026-02-24 02:19
- 分支：feature/full-remediation-2026-02
- 目标：清零阻塞项，使分支达到 PR Merge 水平。

## 本阶段完成内容

1. smart_chunking 向后兼容导出修复
- 文件：`backend/app/services/smart_chunking/__init__.py`
- 变更：补充导出
  - `AcademicStructureDetector`
  - `SemanticChunker`
  - `HierarchicalChunker`
- 目的：恢复 `from app.services.smart_chunking_service import X` 老路径兼容。

2. SmartChunkingService 历史方法兼容
- 文件：`backend/app/services/smart_chunking/service.py`
- 变更：新增 `_empty_result()`，返回空结果结构。
- 目的：兼容历史测试与旧调用约定。

3. HierarchicalChunker 兼容行为修复
- 文件：`backend/app/services/smart_chunking/hierarchical_chunker.py`
- 变更：
  - `_merge_to_sections` 增加“按块数窗口”兼容分支（旧调用传小整数时按块分组）
  - `_generate_chunk_id` 保持 12 位兼容形态
- 目的：通过旧测试快照并维持兼容行为。

4. rate_limit 依赖稳定性修复
- 文件：`backend/app/core/rate_limit.py`
- 变更：`build_rate_limit_dependency` 改为创建依赖级 limiter 实例。
- 目的：避免模块级 limiter 与 monkeypatch 后配置不一致，确保 memory 模式限流可预测。

## 风险评估与回滚

- 风险等级：低到中。
- 兼容性：本次改动以“恢复历史接口/行为兼容”为主。
- 回滚策略：回滚本阶段 commit，即可恢复到修复前状态。
