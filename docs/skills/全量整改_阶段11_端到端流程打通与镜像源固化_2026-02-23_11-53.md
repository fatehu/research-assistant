# 全量整改 阶段11：端到端流程打通与镜像源固化

- 时间：2026-02-23 11:53
- 提交类型：`chore(e2e)`
- 关联范围：`docker-compose.yml`、`.env.example`、`e2e/*` 执行链路

## 目标

- 在当前环境完成“端到端全流程”回归，覆盖 API、前端角色路由、MCP 设置、知识库上传链路。
- 修复知识库文档处理在本地嵌入模型下载阶段的网络阻塞，确保 E2E 可稳定执行。

## 实施内容

1. 启动与复核容器链路
- 启动 `postgres`、`redis`、`codelab-runner`、`backend`、`frontend`。
- 执行 `docker compose ps` 与 `logs` 健康核验。

2. 解决文档处理失败根因
- 日志定位：`knowledge` 文档处理在加载 `BAAI/bge-m3` 时访问 `huggingface.co` TLS EOF，导致 `RetryError`。
- 处置：在 `docker-compose.yml` 的 `backend` 注入 `HF_ENDPOINT`，默认值 `https://hf-mirror.com`。
- 同步在 `.env.example` 增加 `HF_ENDPOINT` 示例项。

3. 全链路 E2E 执行
- API 冒烟：`acceptance_tests.ps1`、`role_business_api_smoke_e2e.ps1`、`remaining_modules_api_smoke_e2e.ps1`。
- 前端角色路由：`student_role_smoke_e2e.ps1`、`role_route_smoke_e2e.ps1 -Role mentor/admin`。
- MCP 设置登录：`mcp_settings_login_e2e.ps1`。
- 知识库上传全链路：`knowledge_upload_pipeline_e2e.ps1`。

## 结果

- 涉及到的端到端流程已在本地完整打通并全部通过。
- 文档上传 -> 处理完成 -> 检索命中链路恢复稳定可回归。
