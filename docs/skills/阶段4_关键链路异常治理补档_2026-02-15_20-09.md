# 阶段4 关键链路异常治理补档

- 时间：2026-02-15 20:09
- 对应提交：`7bd57af feat(api): 切换 /api/v1 并接入限流与统一错误处理`
- 目标：补齐阶段提交对应的 `docs/skills` 证据，满足 `scripts/code.md` 要求。

## 使用技术

1. 统一异常模型 `AppServiceError`
2. 全局错误处理注册 `register_error_handlers(app)`
3. broad catch 增量守卫脚本

## 本阶段做到什么

1. 关键链路统一错误出参与错误码策略。
2. 关键异常转换为业务可解释响应，减少吞错。
3. 增加 `backend/checks/check_no_new_broad_excepts.py` 守卫增量风险。

## 如何感知效果

1. 前端与脚本侧可拿到稳定错误码与 message。
2. 日志定位链路更直接，排障时间下降。
3. 新增 broad catch 可在门禁阶段被拦截。

## 回滚与风险

1. 风险：局部错误码语义变化可能影响调用方提示文案。
2. 回滚：可恢复旧异常映射，但会降低可观测性与可维护性。
