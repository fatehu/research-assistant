# 阶段2 API版本与限流提交流程补档

- 时间：2026-02-15 20:07
- 对应提交：`7bd57af feat(api): 切换 /api/v1 并接入限流与统一错误处理`
- 目标：补齐阶段提交对应的 `docs/skills` 证据，满足 `scripts/code.md` 要求。

## 使用技术

1. FastAPI 路由统一前缀切换到 `/api/v1/*`
2. Redis 优先、内存降级限流依赖
3. 统一限流响应头与 `429` 返回协议

## 本阶段做到什么

1. 后端对外接口统一到 `/api/v1`，移除旧 `/api` 挂载。
2. 分组限流生效：auth/chat/knowledge/codelab。
3. 前端与脚本调用路径同步迁移到 `/api/v1`。

## 如何感知效果

1. `/api/v1/*` 正常服务。
2. `/api/*` 直接 404（按开发期直切策略）。
3. 超限响应含 `X-RateLimit-*` 与 `Retry-After`。

## 回滚与风险

1. 风险：外部脚本若未迁移会出现 404。
2. 回滚：可临时恢复旧前缀兼容路由，但当前策略不建议。
