# 阶段6 CI与E2E门禁化补档

- 时间：2026-02-15 20:11
- 对应提交：
  - `b1a7e4a test(e2e): 新增角色与剩余模块烟雾脚本`
  - `b5610ad ci(workflows): 落地 PR 冒烟与夜间全量回归`
- 目标：补齐阶段提交对应的 `docs/skills` 证据，满足 `scripts/code.md` 要求。

## 使用技术

1. PowerShell E2E 脚本编排（API smoke + UI smoke）
2. GitHub Actions 工作流门禁
3. CI 产物归档（日志与截图）

## 本阶段做到什么

1. 增加 `student_role`、`role_route`、`mcp_settings_login` 等脚本链路。
2. 新增 PR API 冒烟工作流与夜间全量回归工作流。
3. 将 broad catch 守卫与测试产物上传接入工作流。

## 如何感知效果

1. PR 阶段可自动阻断关键接口回归。
2. 夜间任务可覆盖 UI 关键路径并生成截图证据。
3. 故障定位从“口头描述”升级为“日志+截图”闭环。

## 回滚与风险

1. 风险：CI 执行时长提升，夜间资源占用增加。
2. 回滚：可拆分工作流触发条件，保留最小门禁集合。
