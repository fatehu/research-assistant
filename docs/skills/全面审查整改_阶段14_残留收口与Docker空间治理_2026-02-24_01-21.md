# 全面审查整改 阶段14：残留收口与 Docker 空间治理

- 时间：2026-02-24 01:21
- 分支：feature/full-remediation-2026-02
- 目标：
  1. 处理 Docker 磁盘“只增不减”问题，回收可回收空间。
  2. 收口后端残留测试失败（codelab 拆分后的兼容导出问题）。

## 变更概览

1. Docker 空间治理（运维层）
- 执行 `docker builder prune -af` 清理构建缓存。
- 执行 `docker image prune -af` 清理无用镜像。
- 执行 `docker container prune -f` 清理停止容器。
- 结果：镜像/构建缓存合计回收约 17.38GB，显著降低磁盘占用增长压力。

2. 兼容性修复（代码层）
- 文件：`backend/app/api/codelab.py`
- 修复：增加向后兼容导出 `notebook_agent_chat = codelab_agent_routes.notebook_agent_chat`。
- 背景：`codelab` 路由拆分后，历史测试和部分引用仍从 `app.api.codelab` 读取该符号。

3. 测试契约匹配修复（代码层）
- 文件：`backend/app/api/codelab_agent.py`
- 修复：将 assistant 消息 `metadata` 写法恢复为历史断言兼容形式：
  - `metadata={"rag_metrics": rag_metrics} if isinstance(rag_metrics, dict) else {}`
  - 同时保留 `react_steps` 元数据追加逻辑，保证语义不回退。

## 风险与回滚

- 风险评估：低。
- 影响范围：`codelab` Agent 对话路由的导出兼容与消息 metadata 结构构造。
- 回滚方式：
  - 回滚本阶段 commit；
  - 重新执行相关测试，确认回滚后状态。

## 后续建议

1. 将 `docker system df` + prune 命令固化为周期性维护脚本（周级别）。
2. 对 `codelab` 历史导出符号建立兼容层清单，避免后续重构再次触发同类回归。
3. 在 CI 增加“API 模块导出兼容断言”测试，提前发现符号迁移问题。
