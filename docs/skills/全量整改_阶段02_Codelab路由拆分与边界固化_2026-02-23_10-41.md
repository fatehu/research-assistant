# 全量整改阶段02：CodeLab 路由拆分与边界固化实施说明

- 时间：2026-02-23 10:41
- 阶段目标：按职责拆分 `codelab.py`，降低单文件复杂度并保持既有 API 路径不变。
- 关联 commit：`refactor(codelab): 拆分超大路由文件并固化边界`

## 本阶段改动
1. 新增 `backend/app/api/codelab_agent.py`，承接原 `codelab.py` 中的 Notebook Agent 路由、模型与历史管理逻辑。
2. 在 `backend/app/api/codelab.py` 末尾通过 `router.include_router(...)` 挂载新模块路由，保持 `main.py` 中 `/api/v1/codelab` 前缀与外部调用路径不变。
3. 通过 `codelab_base` 复用 `kernel_manager`、`get_notebook_cached`、`_notebooks` 等共享上下文，避免破坏现有导入点与工具链。

## 设计取舍
1. 采用“先拆分后优化”策略：先做到行为等价和边界清晰，再在后续阶段逐步收紧异常与契约。
2. 选择 `include_router` 而非改动主路由注册，目的是降低回归风险。

## 风险与回滚
1. 风险：模块间循环引用风险（已通过将 include 放在 `codelab.py` 文件尾部规避）。
2. 回滚：可整笔回滚到拆分前版本，不涉及数据库变更。

## 效果感知
1. `codelab.py` 文件体量显著下降，Notebook Agent 职责独立。
2. Python 语法编译通过，可进入下一阶段事务与错误语义收敛。
