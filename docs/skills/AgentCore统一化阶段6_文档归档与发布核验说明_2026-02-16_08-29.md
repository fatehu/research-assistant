# AgentCore统一化阶段6：文档归档与发布核验说明（2026-02-16 08:29）

## 1. 本阶段目标
- 归档本轮阶段文档（含 `docs/report`、`docs/skills`、`docs/test`）。
- 完成最终回归与容器发布核验，形成可手测版本。

## 2. 归档范围
- `docs/report/**`
- 本轮新增的 `docs/skills/*.md`
- 本轮新增的 `docs/test/*.md`

## 3. 发布与核验动作
- 使用 `docker compose up -d --build backend frontend` 同步容器到最新代码。
- 通过 `docker compose ps` 与 `docker compose logs --tail 100` 完成健康核验。
- 使用 `.venv-ragtest` 执行快速语法回归，覆盖关键后端链路。

## 4. 结果
- 后端/前端容器均已重建并启动。
- 文档归档完整，阶段提交链路可追溯。

## 5. 风险与后续
- 前端运行在 dev 模式，日志中有历史 HMR 输出，非阻断问题。
- 后续可补一轮 PR 合并前的 E2E 全量回归。