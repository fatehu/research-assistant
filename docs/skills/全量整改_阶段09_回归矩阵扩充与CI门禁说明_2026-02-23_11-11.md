# 全量整改 阶段09：回归矩阵扩充与 CI 门禁说明

- 时间：2026-02-23 11:11
- 提交类型：`test(quality)`
- 关联范围：`backend/checks/*`、`backend/tests/*`、`.github/workflows/*`、`frontend/src/services/api.ts`

## 目标

- 增加可自动执行的“契约一致性”门禁，防止前后端状态与端口默认值漂移。
- 将新门禁纳入 PR 与 Nightly 工作流，形成持续守护。
- 扩充质量回归矩阵并沉淀执行说明。

## 实施内容

- 新增 `backend/checks/check_contract_alignment.py`
- 校验 `frontend/src/services/api.ts` 中 `TaskStatus` 枚举完整性。
- 校验 `normalizeTaskStatus` 的输入别名覆盖与返回状态覆盖。
- 校验 `frontend/src/services/api.ts`、`docker-compose.yml`、`.env.example` 的 `VITE_API_BASE_URL`/`VITE_WS_BASE_URL` 默认值一致性。
- 新增 `backend/tests/test_contract_alignment_check.py`
- 为门禁脚本的解析函数提供纯脚本级单测，减少回归误报。
- 新增 `backend/checks/README.md`
- 记录 checks 目的、推荐执行顺序、失败排查方式。
- 更新 `.github/workflows/pr-api-smoke.yml`
- 新增 `Contract alignment guard` 步骤。
- 更新 `.github/workflows/nightly-e2e-full.yml`
- 新增 `Contract alignment guard` 步骤。
- 更新 `frontend/src/services/api.ts`
- 将 `API_BASE_URL` 兜底值从 `http://localhost:8000` 修正为 `http://localhost:8888`，与 Compose/.env 默认一致。

## 风险与回滚

- 风险：门禁脚本依赖文本结构（正则），若前端文件大幅重构，脚本可能需同步更新。
- 风险：新增门禁会提高 CI 严格度，短期可能暴露历史偏差。
- 回滚：可先移除 workflow 中新增步骤并回退脚本；不影响业务运行逻辑。

## 结果

- 新增门禁已可在本地通过，且已接入两个核心工作流。
- 前后端默认端口契约从“文档对齐”升级为“代码 + 门禁双重对齐”。
